{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Parse email without flattening away useful structure","text":"<p>EMLMailReader v2 turns an EML file or in-memory Internet message into a typed, recursive Python model. It preserves the source bytes and ordered headers, decodes text and transfer encodings, exposes structured addresses and message identifiers, and reports recoverable standards problems as diagnostics.</p> <p>You are reading the v2 documentation</p> <p>These pages describe the EMLMailReader 2.x API. Use the version selector to open the frozen v1.0.4 documentation.</p>"},{"location":"#install","title":"Install","text":"<p>EMLMailReader requires Python 3.12 or newer and has no runtime dependencies outside the Python standard library.</p> <pre><code>python -m pip install \"emlmailreader&gt;=2,&lt;3\"\n</code></pre>"},{"location":"#parse-a-message","title":"Parse a message","text":"<pre><code>from EMLMailReader import MailReader\n\nmessage = MailReader().get_email(\"/data/mail/example.eml\")\nif message is None:\n    raise RuntimeError(\"The file was missing, empty, or unreadable\")\n\nprint(message.Subject)\nprint([mailbox.Email for mailbox in message.From.Mailboxes])\nprint(message.TextBody)\n</code></pre> <p>Use <code>parse_bytes()</code>, <code>parse_string()</code>, or <code>parse_stream()</code> when the message is already in memory. Prefer bytes when exact source preservation matters.</p>"},{"location":"#what-v2-adds","title":"What v2 adds","text":"<ul> <li>ordered, duplicate-preserving headers with raw and decoded values;</li> <li>structured mailboxes, named groups, and empty groups;</li> <li>parsed dates, message identifiers, resent blocks, and trace blocks;</li> <li>one recursive <code>RxMailMessage</code> model for the root and every MIME entity;</li> <li>raw, transfer-decoded, plain-text, and HTML body views;</li> <li>MIME metadata for attachments, inline resources, partial messages, and   external bodies;</li> <li>modern, lenient, and strict parsing modes;</li> <li>stable diagnostics with optional RFC, header, line, and column context;</li> <li>configurable message, header, MIME-depth, part-count, and decoded-body   limits; and</li> <li>a canonical recursive JSON-compatible schema.</li> </ul>"},{"location":"#choose-a-path","title":"Choose a path","text":"<ul> <li>Install and verify v2.</li> <li>Parse your first message.</li> <li>Understand the message model.</li> <li>Build analytics records.</li> <li>Process a directory safely.</li> <li>Migrate code from v1.</li> <li>Open the API reference for exact types and methods.</li> </ul>"},{"location":"concepts/email-mime/","title":"Email and MIME essentials","text":"<p>An EML file combines two related formats:</p> <ul> <li>the Internet Message Format defines fields such as <code>From</code>, <code>Date</code>,   <code>Message-ID</code>, and <code>Received</code>; and</li> <li>MIME defines media types, transfer encodings, multipart boundaries,   attachments, and embedded messages.</li> </ul>"},{"location":"concepts/email-mime/#headers-are-ordered-fields","title":"Headers are ordered fields","text":"<p>Headers are not a dictionary. A message can legally contain repeated fields, field order can matter, and the source spelling may be useful during analysis. <code>HeaderCollection</code> therefore stores one <code>HeaderField</code> per occurrence.</p> <p>Each occurrence retains:</p> <ul> <li>its normalized and original name;</li> <li>folded source text and unfolded text;</li> <li>decoded Unicode text;</li> <li>its position and source line when known; and</li> <li>a current, obsolete, or nonconformant syntax classification.</li> </ul>"},{"location":"concepts/email-mime/#mime-is-a-tree","title":"MIME is a tree","text":"<p>A multipart message is a recursive tree rather than one body plus a flat attachment list. Nested alternatives, forwarded messages, and signed or encrypted structures can contain several levels of child entities.</p> <p>EMLMailReader represents every entity as <code>RxMailMessage</code>, so traversal code does not need different cases for the root, a body part, or an attachment.</p>"},{"location":"concepts/email-mime/#raw-and-interpreted-values-coexist","title":"Raw and interpreted values coexist","text":"<p>Analytics often needs normalized text, while investigations may need exact source bytes. The model retains both whenever they answer different questions. For example, <code>ParsedDateTime</code> keeps the raw field and a Python <code>datetime</code>, and <code>TransferEncodingValue</code> keeps extension tokens instead of replacing them with an assumed standard value.</p>"},{"location":"concepts/email-mime/#diagnostics-preserve-recoverable-mail","title":"Diagnostics preserve recoverable mail","text":"<p>Real mail frequently contains obsolete or malformed syntax. Modern and lenient modes return the recoverable message with diagnostics rather than discarding all data. Strict mode is available when conformance is a hard requirement.</p>"},{"location":"concepts/parser-modes/","title":"Parsing modes, diagnostics, and limits","text":""},{"location":"concepts/parser-modes/#choose-a-parsing-mode","title":"Choose a parsing mode","text":"<pre><code>from EMLMailReader import MailReader, ParsingMode\n\nmodern = MailReader(parsing_mode=ParsingMode.MODERN)\nlenient = MailReader(parsing_mode=ParsingMode.LENIENT)\nstrict = MailReader(parsing_mode=ParsingMode.STRICT)\n</code></pre> Mode Result <code>MODERN</code> Returns recoverable mail and records standards findings <code>LENIENT</code> Receiver-oriented parsing with diagnostics for problematic input <code>STRICT</code> Raises when any error-level diagnostic is collected <p>Modern and lenient currently share the receiver-oriented outcome. Keeping them as distinct modes allows applications to state intent and permits their policy to evolve independently.</p>"},{"location":"concepts/parser-modes/#inspect-diagnostics-recursively","title":"Inspect diagnostics recursively","text":"<pre><code>def diagnostics(message):\n    yield from message.Diagnostics\n    for child in message.Children:\n        yield from diagnostics(child)\n\n\nfor item in diagnostics(message):\n    print(item.code, item.severity.value, item.rfc, item.section)\n</code></pre> <p>Diagnostic codes are the stable value for grouping and automation. Human messages may become more descriptive over time.</p>"},{"location":"concepts/parser-modes/#handle-strict-failures","title":"Handle strict failures","text":"<pre><code>from EMLMailReader import StandardsComplianceError\n\ntry:\n    message = strict.parse_bytes(source)\nexcept StandardsComplianceError as error:\n    for item in error.diagnostics:\n        print(item.code, item.message)\n</code></pre> <p>The exception contains diagnostics from the root and all nested MIME parts.</p>"},{"location":"concepts/parser-modes/#apply-resource-limits","title":"Apply resource limits","text":"<p>Email is untrusted input. Configure bounds appropriate for the dataset and available memory:</p> <pre><code>from EMLMailReader import MailReader, ParserLimits\n\nreader = MailReader(\n    limits=ParserLimits(\n        max_message_bytes=25 * 1024 * 1024,\n        max_header_bytes=512 * 1024,\n        max_header_count=5_000,\n        max_mime_depth=30,\n        max_parts=2_000,\n        max_decoded_part_bytes=10 * 1024 * 1024,\n    )\n)\n</code></pre> <p>Receiver modes attach limit diagnostics and return what could be recovered. Strict mode raises when a limit produces an error-level diagnostic.</p>"},{"location":"concepts/versioning/","title":"Documentation and schema versions","text":"<p>The repository retains source documentation for every supported major release:</p> <pre><code>docs/v1/  EMLMailReader 1.x\ndocs/v2/  EMLMailReader 2.x\n</code></pre> <p>The release workflow builds only the folder matching the package's major version. Mike publishes that build beside existing versions on GitHub Pages.</p>"},{"location":"concepts/versioning/#stable-urls","title":"Stable URLs","text":"<p>Use a major-version URL when reproducibility matters:</p> <pre><code>https://codadel.github.io/EMLMailReader/v2/\n</code></pre> <p>Use <code>/latest/</code> when documentation should follow the current stable package.</p>"},{"location":"concepts/versioning/#package-and-schema-versions","title":"Package and schema versions","text":"<p>The package follows semantic versioning. The canonical exported record also contains <code>schema_version</code>, which identifies the shape of <code>to_dict()</code> and <code>export_as_json()</code> independently from patch releases.</p> <p>EMLMailReader 2.x currently emits schema version <code>2</code>.</p>"},{"location":"getting-started/first-email/","title":"Parse your first message","text":""},{"location":"getting-started/first-email/#read-a-file","title":"Read a file","text":"<pre><code>from pathlib import Path\n\nfrom EMLMailReader import MailReader\n\npath = Path(\"/data/mail/example.eml\")\nmessage = MailReader().get_email(str(path))\n\nif message is None:\n    raise RuntimeError(f\"Could not read {path}\")\n\nprint(message.Subject)\nprint(message.Date.value if message.Date else None)\nprint([mailbox.Email for mailbox in message.From.Mailboxes])\nprint([mailbox.Email for mailbox in message.To.Mailboxes])\n</code></pre> <p><code>get_email()</code> returns <code>None</code> when a file is missing, empty, or cannot be read. Strict standards failures are different: they raise <code>StandardsComplianceError</code>.</p>"},{"location":"getting-started/first-email/#parse-bytes-already-in-memory","title":"Parse bytes already in memory","text":"<pre><code>from EMLMailReader import MailReader\n\nsource = (\n    b\"Date: Fri, 21 Nov 1997 09:55:06 -0600\\r\\n\"\n    b\"From: Alice &lt;alice@example.com&gt;\\r\\n\"\n    b\"To: Analytics: Bob &lt;bob@example.com&gt;, Carol &lt;carol@example.com&gt;;\\r\\n\"\n    b\"Subject: Weekly metrics\\r\\n\"\n    b\"Message-ID: &lt;weekly-42@example.com&gt;\\r\\n\"\n    b\"\\r\\n\"\n    b\"The report is attached.\"\n)\n\nmessage = MailReader().parse_bytes(source)\n\nassert message.RawSource == source\nprint(message.MessageID.value if message.MessageID else None)\nprint(message.Body)\n</code></pre> <p><code>parse_bytes()</code> always returns an <code>RxMailMessage</code> unless strict parsing raises. It is the best input method when exact wire bytes are important.</p>"},{"location":"getting-started/first-email/#other-input-forms","title":"Other input forms","text":"<pre><code>from io import BytesIO, StringIO\n\nfrom EMLMailReader import MailReader\n\nreader = MailReader()\nfrom_text = reader.parse_string(\"From: a@example.com\\r\\n\\r\\nHello\")\nfrom_binary_stream = reader.parse_stream(BytesIO(source))\nfrom_text_stream = reader.parse_stream(StringIO(\"From: a@example.com\\n\\nHello\"))\n</code></pre> <p>Text input is encoded as UTF-8 before parsing. The caller retains ownership of streams and must close them.</p>"},{"location":"getting-started/first-email/#check-diagnostics","title":"Check diagnostics","text":"<pre><code>for diagnostic in message.Diagnostics:\n    print(\n        diagnostic.severity.value,\n        diagnostic.code,\n        diagnostic.header,\n        diagnostic.message,\n    )\n</code></pre> <p>Diagnostics from nested MIME parts remain on their corresponding child nodes. See Parsing modes, diagnostics, and limits.</p>"},{"location":"getting-started/installation/","title":"Install EMLMailReader v2","text":""},{"location":"getting-started/installation/#requirements","title":"Requirements","text":"<ul> <li>Python 3.12 or newer</li> <li>no required runtime packages outside the standard library</li> </ul> <p>Create and activate a virtual environment before installing the library:</p> macOS or LinuxWindows PowerShell <pre><code>python3.12 -m venv .venv\nsource .venv/bin/activate\npython -m pip install --upgrade pip\npython -m pip install \"emlmailreader&gt;=2,&lt;3\"\n</code></pre> <pre><code>py -3.12 -m venv .venv\n.venv\\Scripts\\Activate.ps1\npython -m pip install --upgrade pip\npython -m pip install \"emlmailreader&gt;=2,&lt;3\"\n</code></pre> <p>Pin the major version for production analytics pipelines so a future breaking release cannot change parsed records unexpectedly.</p>"},{"location":"getting-started/installation/#verify-the-public-api","title":"Verify the public API","text":"<pre><code>python -c \"from EMLMailReader import MailReader, ParsingMode; print(ParsingMode.MODERN.value)\"\n</code></pre> <p>The command should print <code>modern</code>.</p>"},{"location":"getting-started/installation/#type-checking","title":"Type checking","text":"<p>The package includes a <code>py.typed</code> marker, so MyPy and other PEP 561-aware tools can inspect its inline annotations without a separate stub package.</p> <pre><code>from EMLMailReader import MailReader, RxMailMessage\n\nmessage: RxMailMessage = MailReader().parse_bytes(\n    b\"From: alice@example.com\\r\\nSubject: Example\\r\\n\\r\\nHello\"\n)\n</code></pre> <p>Continue with Parse your first message.</p>"},{"location":"getting-started/message-model/","title":"Understand the v2 message model","text":"<p>Every parsed result is an <code>RxMailMessage</code>. The root represents the complete Internet message; every child represents another MIME entity using the same class.</p> <pre><code>RxMailMessage (root)\n\u251c\u2500\u2500 headers and Internet-message fields\n\u251c\u2500\u2500 content type and transfer encoding\n\u251c\u2500\u2500 raw and decoded bodies\n\u251c\u2500\u2500 diagnostics\n\u2514\u2500\u2500 Children\n    \u251c\u2500\u2500 RxMailMessage (text/plain)\n    \u251c\u2500\u2500 RxMailMessage (text/html)\n    \u2514\u2500\u2500 RxMailMessage (attachment)\n</code></pre>"},{"location":"getting-started/message-model/#internet-message-fields","title":"Internet-message fields","text":"<p>Common fields have structured projections while all header occurrences remain available through <code>Headers</code>:</p> <pre><code>print(message.Subject)\nprint(message.Date.raw_value if message.Date else None)\nprint(message.Date.value if message.Date else None)\nprint(message.MessageID.value if message.MessageID else None)\n\nfor mailbox in message.From.Mailboxes:\n    print(mailbox.DisplayName, mailbox.Email)\n\nfor field in message.Headers:\n    print(field.index, field.raw_name, field.decoded_value)\n</code></pre> <p><code>Headers</code> preserves duplicates and source order. <code>get()</code> returns the last matching value, while <code>get_all()</code> and <code>occurrences()</code> retain every match.</p>"},{"location":"getting-started/message-model/#bodies-and-raw-data","title":"Bodies and raw data","text":"<p>Each MIME node exposes several views:</p> Property Meaning <code>RawSource</code> Complete source bytes for the entity <code>RawBody</code> Body bytes before transfer decoding <code>DecodedBody</code> Transfer-decoded bytes for a leaf <code>Body</code> Preferred decoded text for the subtree <code>TextBody</code> First <code>text/plain</code> body in the subtree <code>HtmlBody</code> First <code>text/html</code> body in the subtree <p>For <code>multipart/alternative</code>, <code>Body</code> selects the last non-empty alternative. For other multipart containers it selects the first non-empty child body.</p>"},{"location":"getting-started/message-model/#attachments-are-mime-nodes","title":"Attachments are MIME nodes","text":"<p><code>Attachments</code> and <code>InlineResources</code> are recursive tuple views over the same nodes already present in <code>Children</code>:</p> <pre><code>for attachment in message.Attachments:\n    print(\n        attachment.Name,\n        attachment.ContentType.MediaType,\n        len(attachment.DecodedBody),\n    )\n</code></pre> <p>There are no parallel attachment objects to synchronize with the MIME tree.</p>"},{"location":"getting-started/message-model/#serialization","title":"Serialization","text":"<pre><code>record = message.to_dict()\njson_text = message.export_as_json()\n\nassert record[\"schema_version\"] == 2\n</code></pre> <p>The schema includes headers, addresses, dates, identifiers, MIME metadata, bodies, children, and diagnostics. Raw byte payloads are intentionally omitted.</p>"},{"location":"guides/addresses/","title":"Work with addresses and groups","text":"<p><code>From</code>, <code>Sender</code>, <code>ReplyTo</code>, <code>To</code>, <code>Cc</code>, and <code>Bcc</code> are <code>AddressList</code> objects. They preserve top-level mailbox and group boundaries.</p>"},{"location":"guides/addresses/#flatten-participants","title":"Flatten participants","text":"<pre><code>for mailbox in message.To.Mailboxes:\n    print(mailbox.DisplayName, mailbox.Email)\n    print(mailbox.LocalPart, mailbox.Domain)\n</code></pre> <p><code>Mailboxes</code> returns an immutable tuple containing direct and grouped mailboxes.</p>"},{"location":"guides/addresses/#preserve-group-structure","title":"Preserve group structure","text":"<pre><code>from EMLMailReader import AddressGroup\n\nfor item in message.To:\n    if isinstance(item, AddressGroup):\n        print(\"group\", item.display_name)\n        for mailbox in item.addresses:\n            print(\" member\", mailbox.Email)\n    else:\n        print(\"mailbox\", item.Email)\n</code></pre> <p>This distinction matters for values such as:</p> <pre><code>Analytics: alice@example.com, bob@example.com; Undisclosed recipients:;\n</code></pre>"},{"location":"guides/addresses/#normalize-or-serialize","title":"Normalize or serialize","text":"<pre><code>normalized = message.To.to_header_value()\nrecords = message.To.to_dict()\n</code></pre> <p><code>to_dict()</code> emits ordered records with a <code>type</code> of <code>mailbox</code> or <code>group</code>. Internationalized source values are marked on each <code>MailAddress</code> through <code>IsInternationalized</code>.</p>"},{"location":"guides/analytics/","title":"Build stable analytics records","text":"<p>Keep parsing separate from the record shape stored by an analytics system. A small projection function makes schema decisions explicit and keeps raw email objects out of dataframes or databases.</p> <pre><code>from typing import Any\n\nfrom EMLMailReader import RxMailMessage\n\n\ndef analytics_record(message: RxMailMessage, source_name: str) -&gt; dict[str, Any]:\n    return {\n        \"source\": source_name,\n        \"subject\": message.Subject,\n        \"sent_at\": message.Date.value.isoformat()\n        if message.Date and message.Date.value\n        else None,\n        \"message_id\": message.MessageID.value if message.MessageID else None,\n        \"from\": [mailbox.Email for mailbox in message.From.Mailboxes],\n        \"to\": [mailbox.Email for mailbox in message.To.Mailboxes],\n        \"cc\": [mailbox.Email for mailbox in message.Cc.Mailboxes],\n        \"text\": message.TextBody,\n        \"html_available\": bool(message.HtmlBody),\n        \"attachment_count\": len(message.Attachments),\n        \"attachments\": [\n            {\n                \"name\": part.Name,\n                \"media_type\": part.ContentType.MediaType,\n                \"decoded_bytes\": len(part.DecodedBody),\n            }\n            for part in message.Attachments\n        ],\n        \"diagnostic_codes\": sorted(\n            {item.code for item in recursive_diagnostics(message)}\n        ),\n    }\n\n\ndef recursive_diagnostics(message: RxMailMessage):\n    yield from message.Diagnostics\n    for child in message.Children:\n        yield from recursive_diagnostics(child)\n</code></pre>"},{"location":"guides/analytics/#preserve-groups-when-they-matter","title":"Preserve groups when they matter","text":"<p><code>AddressList.Mailboxes</code> is convenient for participant counts. Iterate over the address list itself when named or empty groups are analytically significant.</p>"},{"location":"guides/analytics/#treat-text-as-untrusted-data","title":"Treat text as untrusted data","text":"<p>Parsed bodies, display names, filenames, and subjects can contain sensitive or hostile content. Escape values before rendering HTML, avoid logging message bodies by default, and apply retention rules before storing raw source bytes.</p>"},{"location":"guides/analytics/#choose-the-right-export","title":"Choose the right export","text":"<ul> <li>Use a custom projection for a stable analytics table.</li> <li>Use <code>to_dict()</code> when the complete canonical v2 schema is appropriate.</li> <li>Use <code>export_as_json()</code> when a serialized form of that schema is required.</li> </ul>"},{"location":"guides/attachments/","title":"Work with attachments and inline resources","text":"<p>Attachments are <code>RxMailMessage</code> nodes selected recursively from the MIME tree.</p> <pre><code>for part in message.Attachments:\n    print(part.Name)\n    print(part.ContentType.MediaType)\n    print(part.ContentDisposition.DispositionType if part.ContentDisposition else None)\n    print(len(part.DecodedBody))\n</code></pre> <p>An explicit <code>attachment</code> disposition qualifies. A named MIME entity also qualifies unless it is explicitly inline.</p>"},{"location":"guides/attachments/#inline-resources","title":"Inline resources","text":"<pre><code>for part in message.InlineResources:\n    content_id = part.ContentID.value if part.ContentID else None\n    print(content_id, part.Name, part.ContentType.MediaType)\n</code></pre> <p>Inline resources remain in <code>Children</code>; the tuple is only a recursive view.</p>"},{"location":"guides/attachments/#save-decoded-attachments","title":"Save decoded attachments","text":"<pre><code>from pathlib import Path\n\ntarget = Path(\"/data/extracted/example\")\ntarget.mkdir(parents=True, exist_ok=True)\nmessage.save_attachments(str(target))\n</code></pre> <p>The library requires the destination to exist, reduces filenames to their basename, and overwrites collisions. If collision handling or content scanning matters, iterate over <code>Attachments</code> and implement an application-specific save policy instead.</p> <p>Never trust a filename, media type, or extension solely because it appeared in an email header.</p>"},{"location":"guides/batch-processing/","title":"Process a directory of EML files","text":"<p>Reuse one configured <code>MailReader</code> across files and record failures separately from successfully parsed messages.</p> <pre><code>from collections.abc import Iterator\nfrom pathlib import Path\n\nfrom EMLMailReader import MailReader, ParserLimits, RxMailMessage\n\n\ndef read_mailbox(root: Path) -&gt; Iterator[tuple[Path, RxMailMessage]]:\n    reader = MailReader(\n        limits=ParserLimits(\n            max_message_bytes=25 * 1024 * 1024,\n            max_mime_depth=30,\n            max_parts=2_000,\n        )\n    )\n\n    for path in sorted(root.rglob(\"*.eml\")):\n        message = reader.get_email(str(path))\n        if message is not None:\n            yield path, message\n\n\nfor path, message in read_mailbox(Path(\"/data/export\")):\n    print(path, message.Subject, len(message.Attachments))\n</code></pre>"},{"location":"guides/batch-processing/#operational-recommendations","title":"Operational recommendations","text":"<ul> <li>Treat <code>None</code> as a file-read outcome, not an empty parsed message.</li> <li>Store the source path or another stable ingestion identifier.</li> <li>Apply explicit limits before processing external mailboxes.</li> <li>Aggregate diagnostic codes and severities rather than logging entire   messages.</li> <li>Avoid loading a directory's complete parsed result set into memory when a   generator or streaming write is sufficient.</li> <li>Decide how duplicate input files will be detected before rerunning a batch.</li> </ul> <p>Use strict mode for validation jobs that should reject nonconformant input. For analytics, modern mode usually retains more useful evidence.</p>"},{"location":"guides/exporting/","title":"Export parsed data","text":""},{"location":"guides/exporting/#canonical-dictionary","title":"Canonical dictionary","text":"<pre><code>record = message.to_dict()\nassert record[\"schema_version\"] == 2\n</code></pre> <p>The record contains structured headers, addresses, date and identifier values, MIME metadata, derived text bodies, recursive children, and diagnostics.</p> <p>Raw byte properties are deliberately excluded. This avoids accidental payload duplication and keeps the result JSON-compatible.</p>"},{"location":"guides/exporting/#json-text","title":"JSON text","text":"<pre><code>json_text = message.export_as_json()\n</code></pre> <p>The serializer preserves Unicode instead of forcing non-ASCII characters into escape sequences.</p>"},{"location":"guides/exporting/#json-lines-example","title":"JSON Lines example","text":"<pre><code>from pathlib import Path\n\nfrom EMLMailReader import MailReader\n\nreader = MailReader()\nwith Path(\"messages.jsonl\").open(\"w\", encoding=\"utf-8\") as output:\n    for path in sorted(Path(\"/data/mail\").glob(\"*.eml\")):\n        message = reader.get_email(str(path))\n        if message is not None:\n            output.write(message.export_as_json())\n            output.write(\"\\n\")\n</code></pre> <p>For long-lived datasets, consider projecting a smaller application-owned schema so library additions do not create unexpected columns.</p>"},{"location":"guides/headers/","title":"Inspect ordered and repeated headers","text":"<p><code>HeaderCollection</code> behaves like a sequence of <code>HeaderField</code> occurrences and also provides case-insensitive lookup.</p> <pre><code># Last decoded Subject value, or None.\nsubject = message.Headers.get(\"subject\")\n\n# Every Received value in source order, without encoded-word decoding.\nreceived = message.Headers.get_all(\"Received\", decoded=False)\n\n# Complete structured occurrences.\nfor field in message.Headers.occurrences(\"X-Tracking-ID\"):\n    print(field.index, field.raw_name, field.raw_value)\n</code></pre>"},{"location":"guides/headers/#choose-the-appropriate-representation","title":"Choose the appropriate representation","text":"Property Use <code>raw_value</code> Preserve folded source text <code>unfolded_value</code> Inspect source semantics without folding <code>decoded_value</code> Work with interpreted Unicode text <code>syntax_status</code> Separate current, obsolete, and invalid syntax <p>Do not convert all headers into a normal dictionary when duplicate values or source order matter. Use <code>Headers.to_list()</code> for a JSON-compatible ordered representation.</p> <p>Structured projections such as <code>Date</code>, <code>MessageID</code>, <code>ResentBlocks</code>, and <code>TraceBlocks</code> complement the header collection; they do not replace it.</p>"},{"location":"guides/logging-errors/","title":"Handle logging and failures","text":""},{"location":"guides/logging-errors/#file-reading-outcomes","title":"File-reading outcomes","text":"<p><code>get_email()</code> returns <code>None</code> for missing, empty, or unreadable files. Parsing bytes, strings, or streams does not use that sentinel.</p>"},{"location":"guides/logging-errors/#standards-failures","title":"Standards failures","text":"<p>Strict mode raises <code>StandardsComplianceError</code>; its <code>diagnostics</code> tuple contains the complete root and nested findings collected before failure.</p>"},{"location":"guides/logging-errors/#optional-parser-logging","title":"Optional parser logging","text":"<p>Logging is disabled by default. Use Python's logging-backed console or file mode when operational diagnostics must be emitted:</p> <pre><code>from EMLMailReader import LoggingMode, MailReader\n\nconsole_reader = MailReader(logging_mode=LoggingMode.CONSOLE)\nfile_reader = MailReader(\n    logging_mode=LoggingMode.FILE,\n    TargetLoggingFolder=\"/existing/log/directory\",\n)\n</code></pre> <p>The target directory must already exist for file logging. Logger configuration is process-wide, so applications should configure it deliberately rather than construct competing readers with different destinations.</p> <p>Avoid logging message bodies, raw source, authentication tokens, or complete address lists unless the application's privacy policy explicitly permits it.</p>"},{"location":"migration/v1-to-v2/","title":"Migrate from v1 to v2","text":"<p>EMLMailReader v2 replaces the flattened v1 model with one structured, standards-aware message and MIME model. Treat the upgrade as a schema migration, not a drop-in patch update.</p>"},{"location":"migration/v1-to-v2/#package-installation","title":"Package installation","text":"<pre><code>python -m pip install --upgrade \"emlmailreader&gt;=2,&lt;3\"\n</code></pre> <p>Pin v1 while migrating production code:</p> <pre><code>python -m pip install \"emlmailreader==1.0.4\"\n</code></pre>"},{"location":"migration/v1-to-v2/#major-api-changes","title":"Major API changes","text":"v1 v2 <code>MailAddressCollection</code> <code>AddressList</code> <code>MailAttachment</code> objects Attachment <code>RxMailMessage</code> nodes <code>MailAttachmentCollection</code> Immutable <code>message.Attachments</code> tuple dictionary-like flattened headers Ordered <code>HeaderCollection</code> string <code>Date</code> <code>ParsedDateTime</code> or <code>None</code> string <code>MessageID</code> <code>ParsedMessageID</code> or <code>None</code> enum-only transfer encoding Lossless <code>TransferEncodingValue</code> shallow JSON metadata Recursive canonical schema version 2 file-only parsing Files, bytes, strings, and streams limited failure information Structured diagnostics and strict mode"},{"location":"migration/v1-to-v2/#address-collections","title":"Address collections","text":"<p>Before:</p> <pre><code>recipients = message.To.export_as_list()\ncount = message.To.length()\n</code></pre> <p>After:</p> <pre><code>recipients = message.To.Mailboxes\ncount = len(message.To)\nemails = [mailbox.Email for mailbox in message.To.Mailboxes]\n</code></pre> <p><code>len(message.To)</code> counts top-level mailboxes and groups. Use <code>len(message.To.Mailboxes)</code> for the flattened mailbox count.</p>"},{"location":"migration/v1-to-v2/#attachments","title":"Attachments","text":"<p>Before:</p> <pre><code>for attachment in message.Attachments.export_as_list():\n    print(attachment.Name, len(attachment.Contents))\n</code></pre> <p>After:</p> <pre><code>for attachment in message.Attachments:\n    print(attachment.Name, len(attachment.DecodedBody))\n</code></pre> <p>Attachments are no longer copied into parallel model classes. They are the same MIME nodes found by traversing <code>Children</code>.</p>"},{"location":"migration/v1-to-v2/#dates-and-identifiers","title":"Dates and identifiers","text":"<p>Before:</p> <pre><code>sent_at = message.Date\nidentifier = message.MessageID\n</code></pre> <p>After:</p> <pre><code>sent_at = message.Date.value if message.Date else None\nraw_date = message.Date.raw_value if message.Date else None\nidentifier = message.MessageID.value if message.MessageID else None\n</code></pre> <p>Check <code>valid</code> when input quality matters.</p>"},{"location":"migration/v1-to-v2/#headers","title":"Headers","text":"<p>Before, repeated fields could be flattened or overwritten. In v2:</p> <pre><code>last_subject = message.Headers.get(\"Subject\")\nall_received = message.Headers.get_all(\"Received\", decoded=False)\nsubject_occurrences = message.Headers.occurrences(\"Subject\")\n</code></pre> <p>Do not replace <code>HeaderCollection</code> with <code>dict(message.Headers)</code>; duplicate names and order are part of the model.</p>"},{"location":"migration/v1-to-v2/#json-export","title":"JSON export","text":"<p>Before:</p> <pre><code>json_text = message.export_as_json()\n</code></pre> <p>The method still exists, but its output is intentionally different. v2 emits a recursive record with <code>schema_version == 2</code>. Update warehouse schemas, fixtures, and downstream transformations before switching production data.</p>"},{"location":"migration/v1-to-v2/#parsing-policy","title":"Parsing policy","text":"<p>Configure explicit limits and decide whether recoverable diagnostics are data quality signals or hard failures:</p> <pre><code>from EMLMailReader import MailReader, ParserLimits, ParsingMode\n\nreader = MailReader(\n    parsing_mode=ParsingMode.MODERN,\n    limits=ParserLimits(max_message_bytes=25 * 1024 * 1024),\n)\n</code></pre> <p>Use strict mode only when rejecting any error-level standards finding is the desired application policy.</p>"},{"location":"migration/v1-to-v2/#recommended-migration-sequence","title":"Recommended migration sequence","text":"<ol> <li>Pin the existing production environment to v1.0.4.</li> <li>Add v2 to a separate test environment.</li> <li>Replace collection helper methods and attachment classes.</li> <li>Update date, identifier, header, and JSON handling.</li> <li>Add limits and diagnostic collection.</li> <li>Compare analytics records from representative real mail.</li> <li>Update stored schema documentation before changing production ingestion.</li> </ol>"},{"location":"reference/","title":"API reference","text":"<p>The reference is split into two layers:</p> <ul> <li>focused pages explain how each public type behaves; and</li> <li>generated pages show signatures and docstrings from the v2 source code.</li> </ul>"},{"location":"reference/#parsing-and-message-model","title":"Parsing and message model","text":"<ul> <li>MailReader</li> <li>RxMailMessage</li> <li>Generated core API</li> </ul>"},{"location":"reference/#addresses-headers-and-structured-values","title":"Addresses, headers, and structured values","text":"<ul> <li>MailAddress</li> <li>AddressList</li> <li>Structured types</li> <li>Generated structured API</li> </ul>"},{"location":"reference/#mime-values","title":"MIME values","text":"<ul> <li>ContentType</li> <li>ContentDisposition</li> <li>Generated MIME API</li> </ul>"},{"location":"reference/#utilities-and-failures","title":"Utilities and failures","text":"<ul> <li>TextEncoding</li> <li>Logger</li> <li>Enumerations</li> <li>Exceptions</li> <li>Generated utility API</li> </ul> <p>All supported public symbols are exported from <code>EMLMailReader</code>, except the internal <code>LoggingLevel</code> enum.</p>"},{"location":"reference/AddressList/","title":"AddressList","text":"<p><code>AddressList</code> represents a complete RFC-style address header value. It retains both individual <code>MailAddress</code> objects and named or empty <code>AddressGroup</code> objects. <code>RxMailMessage.From</code>, <code>Sender</code>, <code>ReplyTo</code>, <code>To</code>, <code>Cc</code>, and <code>Bcc</code> all use this type.</p>"},{"location":"reference/AddressList/#constructor-and-parsing","title":"Constructor and parsing","text":"<pre><code>AddressList(raw_value: str = \"\")\n</code></pre> <p>Supplying a value parses it immediately.</p> <pre><code>from EMLMailReader import AddressList\n\nrecipients = AddressList(\n    \"Alice &lt;alice@example.com&gt;, Engineering: Bob &lt;bob@example.com&gt;, carol@example.com;\"\n)\n</code></pre> <p>An existing object can be reused:</p> <pre><code>recipients.parse(\"Undisclosed recipients:;\")\n</code></pre> <pre><code>parse(value: str) -&gt; AddressList\n</code></pre> <p><code>parse()</code> replaces <code>RawValue</code> and <code>Items</code>, then returns the same object.</p>"},{"location":"reference/AddressList/#properties","title":"Properties","text":"Property Type Description <code>RawValue</code> <code>str</code> Complete input header value <code>Items</code> <code>list[MailAddress \\| AddressGroup]</code> Top-level mailboxes and groups <code>Mailboxes</code> <code>tuple[MailAddress, ...]</code> Flattened immutable view of every group member and standalone mailbox <p><code>AddressGroup</code> is an exported frozen data class with:</p> Field Type <code>display_name</code> <code>str</code> <code>addresses</code> <code>tuple[MailAddress, ...]</code> <code>raw_value</code> <code>str</code> <pre><code>from EMLMailReader import AddressGroup\n\nfor item in recipients:\n    if isinstance(item, AddressGroup):\n        print(item.display_name)\n        for mailbox in item.addresses:\n            print(mailbox.Email)\n    else:\n        print(item.Email)\n</code></pre>"},{"location":"reference/AddressList/#collection-behavior","title":"Collection behavior","text":"<p><code>AddressList</code> supports:</p> <ul> <li>iteration over top-level <code>Items</code></li> <li><code>len(addresses)</code> for the number of top-level items</li> <li>truth testing</li> </ul> <p>Truth testing is true when parsed items exist or a non-empty raw value was supplied. <code>len()</code> does not return the flattened mailbox count; use <code>len(addresses.Mailboxes)</code> for that.</p>"},{"location":"reference/AddressList/#formatting","title":"Formatting","text":"<pre><code>value = recipients.to_header_value()\nsame_value = str(recipients)\n</code></pre> <p>Named and empty groups are serialized with group syntax. Individual addresses delegate to <code>MailAddress.to_header_value()</code>.</p>"},{"location":"reference/AddressList/#serialization","title":"Serialization","text":"<pre><code>data = recipients.to_dict()\n</code></pre> <p>The result is a list. Each entry has a discriminator:</p> <pre><code>[\n    {\n        \"type\": \"mailbox\",\n        \"display_name\": \"Alice\",\n        \"email\": \"alice@example.com\",\n        \"local_part\": \"alice\",\n        \"domain\": \"example.com\",\n        \"raw_value\": \"Alice &lt;alice@example.com&gt;\",\n        \"internationalized\": False,\n    },\n    {\n        \"type\": \"group\",\n        \"display_name\": \"Engineering\",\n        \"addresses\": [\n            {\n                \"display_name\": \"Bob\",\n                \"email\": \"bob@example.com\",\n                \"local_part\": \"bob\",\n                \"domain\": \"example.com\",\n                \"raw_value\": \"Bob &lt;bob@example.com&gt;\",\n                \"internationalized\": False,\n            },\n            {\n                \"display_name\": \"\",\n                \"email\": \"carol@example.com\",\n                \"local_part\": \"carol\",\n                \"domain\": \"example.com\",\n                \"raw_value\": \"carol@example.com\",\n                \"internationalized\": False,\n            },\n        ],\n        \"raw_value\": (\n            \"Alice &lt;alice@example.com&gt;, \"\n            \"Engineering: Bob &lt;bob@example.com&gt;, carol@example.com;\"\n        ),\n    },\n]\n</code></pre>"},{"location":"reference/AddressList/#related-pages","title":"Related pages","text":"<ul> <li>MailAddress</li> <li>RxMailMessage</li> <li>Structured types</li> </ul>"},{"location":"reference/ContentDisposition/","title":"ContentDisposition","text":"<p><code>ContentDisposition</code> stores the disposition and decoded parameters from a Content-Disposition header. <code>RxMailMessage.ContentDisposition</code> is <code>None</code> when the header is absent.</p>"},{"location":"reference/ContentDisposition/#properties","title":"Properties","text":"Property Type Description <code>DispositionType</code> <code>str</code> Lowercase registered or extension token <code>FileName</code> <code>str</code> Decoded filename parameter <code>CreationDate</code> <code>ParsedDateTime \\| None</code> Parsed <code>creation-date</code> parameter <code>ModificationDate</code> <code>ParsedDateTime \\| None</code> Parsed <code>modification-date</code> parameter <code>ReadDate</code> <code>ParsedDateTime \\| None</code> Parsed <code>read-date</code> parameter <code>Size</code> <code>int</code> Numeric size parameter, or <code>0</code> when absent/invalid <code>Parameters</code> <code>dict[str, str]</code> All decoded parameters with lowercase keys <code>RawValue</code> <code>str</code> Header value supplied to <code>parse()</code> <code>IsExplicit</code> <code>bool</code> True when a non-empty value was supplied"},{"location":"reference/ContentDisposition/#parsing","title":"Parsing","text":"<pre><code>from EMLMailReader import ContentDisposition\n\ndisposition = ContentDisposition()\ndisposition.parse(\n    'attachment; filename=\"report.pdf\"; size=1200; '\n    'creation-date=\"Fri, 21 Nov 1997 09:55:06 -0600\"'\n)\n\nassert disposition.DispositionType == \"attachment\"\nassert disposition.FileName == \"report.pdf\"\nassert disposition.Size == 1200\nassert disposition.CreationDate.valid\n</code></pre> <pre><code>parse(ContentDispositionString: str) -&gt; None\n</code></pre> <p>Each call replaces the parsed state. Extension disposition tokens and parameters are retained rather than rejected.</p> <p>Date parameters produce <code>ParsedDateTime</code> even when invalid; inspect its <code>valid</code> and <code>value</code> fields.</p>"},{"location":"reference/ContentDisposition/#parameter-lookup","title":"Parameter lookup","text":"<pre><code>value = disposition.get_parameter(\"filename\", default=\"\")\n</code></pre> <p>Lookup is case-insensitive.</p>"},{"location":"reference/ContentDisposition/#header-and-dictionary-forms","title":"Header and dictionary forms","text":"<pre><code>header_value = disposition.to_header_value()\nsame_value = str(disposition)\ndata = disposition.to_dict()\n</code></pre> <p>The dictionary contains:</p> <pre><code>{\n    \"type\": \"attachment\",\n    \"filename\": \"report.pdf\",\n    \"creation_date\": {\n        \"raw_value\": \"Fri, 21 Nov 1997 09:55:06 -0600\",\n        \"value\": \"1997-11-21T09:55:06-06:00\",\n        \"valid\": True,\n        \"obsolete\": False,\n    },\n    \"modification_date\": None,\n    \"read_date\": None,\n    \"size\": 1200,\n    \"parameters\": {\n        \"filename\": \"report.pdf\",\n        \"size\": \"1200\",\n        \"creation-date\": \"Fri, 21 Nov 1997 09:55:06 -0600\",\n    },\n    \"raw_value\": (\n        'attachment; filename=\"report.pdf\"; size=1200; '\n        'creation-date=\"Fri, 21 Nov 1997 09:55:06 -0600\"'\n    ),\n    \"is_explicit\": True,\n}\n</code></pre>"},{"location":"reference/ContentDisposition/#attachment-classification","title":"Attachment classification","text":"<p><code>RxMailMessage.IsInline</code> checks for <code>DispositionType == \"inline\"</code>. <code>RxMailMessage.IsAttachment</code> checks for <code>attachment</code>, or for a named part that is not inline. The attachment itself remains an <code>RxMailMessage</code>.</p>"},{"location":"reference/ContentDisposition/#related-pages","title":"Related pages","text":"<ul> <li>RxMailMessage</li> <li>ContentType</li> <li>Structured types</li> </ul>"},{"location":"reference/ContentType/","title":"ContentType","text":"<p><code>ContentType</code> stores the effective MIME media type and all decoded parameters for one <code>RxMailMessage</code> node.</p>"},{"location":"reference/ContentType/#default-state","title":"Default state","text":"<pre><code>from EMLMailReader import ContentType\n\ncontent_type = ContentType()\n</code></pre> Property Default Description <code>MediaType</code> <code>\"text/plain\"</code> Lowercase type and subtype <code>Charset</code> <code>\"us-ascii\"</code> Text charset or MIME default <code>Boundary</code> <code>\"\"</code> Multipart boundary <code>Name</code> <code>\"\"</code> Decoded <code>name</code> parameter <code>Parameters</code> <code>{}</code> All decoded, lowercase-keyed parameters <code>RawValue</code> <code>\"\"</code> Original header value passed to <code>parse()</code> <code>IsExplicit</code> <code>False</code> Whether a Content-Type header value was supplied"},{"location":"reference/ContentType/#parsing","title":"Parsing","text":"<pre><code>parse(\n    ContentTypeString: str | None = None,\n    *,\n    effective_media_type: str | None = None,\n) -&gt; None\n</code></pre> <pre><code>content_type.parse('multipart/mixed; boundary=\"part-boundary\"; name=\"message.eml\"')\n\nassert content_type.MediaType == \"multipart/mixed\"\nassert content_type.Boundary == \"part-boundary\"\nassert content_type.Name == \"message.eml\"\nassert content_type.IsExplicit\n</code></pre> <p>MIME parameters, including decoded extended parameters handled by Python's email package, are exposed in <code>Parameters</code>.</p> <p>When no explicit value is supplied, <code>effective_media_type</code> can provide the parser's context-sensitive default. Otherwise the default is <code>text/plain</code>. <code>RawValue</code> remains empty and <code>IsExplicit</code> remains false for an effective default.</p>"},{"location":"reference/ContentType/#parameter-lookup","title":"Parameter lookup","text":"<pre><code>format_value = content_type.get_parameter(\"FORMAT\", default=None)\n</code></pre> <p>Parameter names are case-insensitive because lookup lowercases the requested name.</p>"},{"location":"reference/ContentType/#header-and-dictionary-forms","title":"Header and dictionary forms","text":"<pre><code>header_value = content_type.to_header_value()\nsame_value = str(content_type)\ndata = content_type.to_dict()\n</code></pre> <p><code>to_dict()</code> returns:</p> <pre><code>{\n    \"media_type\": \"multipart/mixed\",\n    \"charset\": \"us-ascii\",\n    \"boundary\": \"part-boundary\",\n    \"name\": \"message.eml\",\n    \"parameters\": {\n        \"boundary\": \"part-boundary\",\n        \"name\": \"message.eml\",\n    },\n    \"raw_value\": 'multipart/mixed; boundary=\"part-boundary\"; name=\"message.eml\"',\n    \"is_explicit\": True,\n}\n</code></pre>"},{"location":"reference/ContentType/#related-pages","title":"Related pages","text":"<ul> <li>RxMailMessage</li> <li>ContentDisposition</li> </ul>"},{"location":"reference/Enumerations/","title":"Enumerations","text":"<p>EMLMailReader exposes enums for transfer encodings, parser policy, syntax classification, diagnostic severity, and logging.</p>"},{"location":"reference/Enumerations/#imports","title":"Imports","text":"<pre><code>from EMLMailReader import (\n    DiagnosticSeverity,\n    LoggingMode,\n    ParsingMode,\n    SyntaxStatus,\n    TransferEncoding,\n)\nfrom EMLMailReader.Enumerations import LoggingLevel\n</code></pre> <p><code>LoggingLevel</code> is used by the low-level <code>Logger.logentry()</code> API and is not re-exported from the package root.</p>"},{"location":"reference/Enumerations/#transferencoding","title":"TransferEncoding","text":"<p>String enum used by <code>TransferEncodingValue.kind</code>.</p> Member Value <code>BASE64</code> <code>\"base64\"</code> <code>SEVEN_BIT</code> <code>\"7bit\"</code> <code>EIGHT_BIT</code> <code>\"8bit\"</code> <code>QUOTED_PRINTABLE</code> <code>\"quoted-printable\"</code> <code>BINARY</code> <code>\"binary\"</code> <code>UNKNOWN</code> <code>\"unknown\"</code> <p>Unknown wire tokens are represented by <code>UNKNOWN</code> while the original lowercase token remains available in <code>TransferEncodingValue.raw_value</code>.</p>"},{"location":"reference/Enumerations/#loggingmode","title":"LoggingMode","text":"Member Numeric value Behavior <code>CONSOLE</code> 1 Configure root logging to the console <code>FILE</code> 2 Configure root logging to a timestamped file <code>NONE</code> 3 Do not configure logging <pre><code>reader = MailReader(logging_mode=LoggingMode.CONSOLE)\n</code></pre>"},{"location":"reference/Enumerations/#logginglevel","title":"LoggingLevel","text":"Member Numeric value <code>DEBUG</code> 1 <code>INFO</code> 2 <code>ERROR</code> 3 <code>CRITICAL</code> 4 <p><code>Logger.logentry()</code> routes unknown levels through <code>logging.debug()</code>.</p>"},{"location":"reference/Enumerations/#parsingmode","title":"ParsingMode","text":"<p>String enum accepted by <code>MailReader</code>:</p> Member Value <code>MODERN</code> <code>\"modern\"</code> <code>LENIENT</code> <code>\"lenient\"</code> <code>STRICT</code> <code>\"strict\"</code> <p>See Structured types for mode behavior.</p>"},{"location":"reference/Enumerations/#syntaxstatus","title":"SyntaxStatus","text":"<p>String enum stored in <code>HeaderField.syntax_status</code>:</p> Member Value <code>CURRENT</code> <code>\"current\"</code> <code>OBSOLETE</code> <code>\"obsolete\"</code> <code>NONCONFORMANT</code> <code>\"nonconformant\"</code>"},{"location":"reference/Enumerations/#diagnosticseverity","title":"DiagnosticSeverity","text":"<p>String enum stored in <code>ParseDiagnostic.severity</code>:</p> Member Value <code>INFO</code> <code>\"info\"</code> <code>WARNING</code> <code>\"warning\"</code> <code>ERROR</code> <code>\"error\"</code>"},{"location":"reference/Enumerations/#removed-legacy-enums","title":"Removed legacy enums","text":"<p>The current API does not expose <code>EntityType</code> or <code>DispositionType</code>. <code>ContentDisposition.DispositionType</code> is a string so registered and extension tokens can both be retained.</p>"},{"location":"reference/Enumerations/#related-pages","title":"Related pages","text":"<ul> <li>Structured types</li> <li>Logger</li> <li>ContentDisposition</li> </ul>"},{"location":"reference/Exceptions/","title":"Exceptions","text":"<p>The current public API exports three exception classes.</p> <pre><code>from EMLMailReader import (\n    FileMissingError,\n    FolderNotAvailableError,\n    StandardsComplianceError,\n)\n</code></pre>"},{"location":"reference/Exceptions/#standardscomplianceerror","title":"StandardsComplianceError","text":"<pre><code>StandardsComplianceError(diagnostics)\n</code></pre> <p>Raised by <code>MailReader</code> in <code>ParsingMode.STRICT</code> when any root or nested parser diagnostic has error severity.</p> <p>The <code>diagnostics</code> attribute is an immutable tuple containing the complete flattened diagnostic report, not only errors.</p> <pre><code>from EMLMailReader import MailReader, ParsingMode, StandardsComplianceError\n\ntry:\n    message = MailReader(parsing_mode=ParsingMode.STRICT).parse_bytes(source)\nexcept StandardsComplianceError as error:\n    for diagnostic in error.diagnostics:\n        print(\n            diagnostic.severity.value,\n            diagnostic.code,\n            diagnostic.message,\n        )\n</code></pre> <p>The exception message lists the error diagnostic codes.</p>"},{"location":"reference/Exceptions/#foldernotavailableerror","title":"FolderNotAvailableError","text":"<pre><code>FolderNotAvailableError(folderPath: str)\n</code></pre> <p>Raised when:</p> <ul> <li><code>Logger.set_configuration(LoggingMode.FILE, ...)</code> receives a missing or empty   target</li> <li><code>RxMailMessage.save_attachments(...)</code> receives a path that is not an existing   directory</li> </ul> <p>The supplied path is available through <code>folderPath</code>.</p> <pre><code>from EMLMailReader import FolderNotAvailableError\n\ntry:\n    message.save_attachments(\"/missing/output\")\nexcept FolderNotAvailableError as error:\n    print(error.folderPath)\n</code></pre>"},{"location":"reference/Exceptions/#filemissingerror","title":"FileMissingError","text":"<pre><code>FileMissingError(filePath: str)\n</code></pre> <p>Carries the unavailable path in <code>filePath</code>. <code>MailReader.get_email()</code> uses this exception internally but catches it with other filesystem read failures, logs the error when configured, and returns <code>None</code>. Normal callers therefore check the return value:</p> <pre><code>message = MailReader().get_email(\"/missing/message.eml\")\nif message is None:\n    print(\"Message was not available\")\n</code></pre>"},{"location":"reference/Exceptions/#exceptions-no-longer-present","title":"Exceptions no longer present","text":"<p>The current package does not define the previous <code>InvalidEncodingError</code>, <code>IncompleteHeaderError</code>, or <code>InvalidPropertyError</code> classes. Encoding and syntax problems are represented by parser diagnostics or by the standard Python exception raised by a direct utility call.</p>"},{"location":"reference/Exceptions/#related-pages","title":"Related pages","text":"<ul> <li>MailReader</li> <li>RxMailMessage</li> <li>Logger</li> <li>Structured types</li> </ul>"},{"location":"reference/Logger/","title":"Logger","text":"<p><code>Logger</code> is a static wrapper around Python's process-wide root logger. <code>MailReader</code> uses it to report parser diagnostics and filesystem read failures.</p> <p>Applications normally configure logging through the <code>MailReader</code> constructor.</p>"},{"location":"reference/Logger/#configure-through-mailreader","title":"Configure through MailReader","text":"<pre><code>from EMLMailReader import LoggingMode, MailReader\n\nconsole_reader = MailReader(logging_mode=LoggingMode.CONSOLE)\n\nfile_reader = MailReader(\n    logging_mode=LoggingMode.FILE,\n    TargetLoggingFolder=\"/existing/log/directory\",\n)\n</code></pre> <p><code>LoggingMode.NONE</code> is the default and leaves logging unconfigured.</p>"},{"location":"reference/Logger/#set_configuration","title":"<code>set_configuration()</code>","text":"<pre><code>Logger.set_configuration(\n    logging_mode: LoggingMode,\n    target_folder: str = \"\",\n) -&gt; str\n</code></pre>"},{"location":"reference/Logger/#console-mode","title":"Console mode","text":"<pre><code>from EMLMailReader import Logger, LoggingMode\n\nresult = Logger.set_configuration(LoggingMode.CONSOLE)\nassert result == \"\"\n</code></pre> <p>Configures the root logger at <code>DEBUG</code> level with this format:</p> <pre><code>timestamp level logger-name message\n</code></pre>"},{"location":"reference/Logger/#file-mode","title":"File mode","text":"<pre><code>log_path = Logger.set_configuration(\n    LoggingMode.FILE,\n    \"/existing/log/directory\",\n)\nprint(log_path)\n</code></pre> <p>The target must already exist. The method creates a UTF-8 log named like:</p> <pre><code>EMLMailReader_Logs_2026712_143055.log\n</code></pre> <p>It returns the complete log path. A missing or empty target raises <code>FolderNotAvailableError</code>.</p> <p>The filename has one-second timestamp precision. Reconfiguring file logging more than once in the same second can select the same filename.</p>"},{"location":"reference/Logger/#none-mode","title":"None mode","text":"<pre><code>result = Logger.set_configuration(LoggingMode.NONE)\nassert result == \"\"\n</code></pre> <p>No logging configuration is changed.</p>"},{"location":"reference/Logger/#process-wide-effect","title":"Process-wide effect","text":"<p>Console and file modes call <code>logging.basicConfig(..., force=True)</code>. They replace existing root handlers and affect the entire Python process, not only EMLMailReader. Applications that need centralized logging control can keep <code>LoggingMode.NONE</code> and configure Python logging themselves.</p>"},{"location":"reference/Logger/#logentry","title":"<code>logentry()</code>","text":"<pre><code>Logger.logentry(message: str, logging_level: LoggingLevel) -&gt; None\n</code></pre> <pre><code>from EMLMailReader import Logger\nfrom EMLMailReader.Enumerations import LoggingLevel\n\nLogger.logentry(\"Message parsed\", LoggingLevel.INFO)\nLogger.logentry(\"Message could not be read\", LoggingLevel.ERROR)\n</code></pre> <p>Routing:</p> Level Python logging call <code>LoggingLevel.INFO</code> <code>logging.info()</code> <code>LoggingLevel.ERROR</code> <code>logging.error()</code> <code>LoggingLevel.CRITICAL</code> <code>logging.critical()</code> <code>LoggingLevel.DEBUG</code> or another value <code>logging.debug()</code> <p><code>LoggingLevel</code> is imported from <code>EMLMailReader.Enumerations</code>; it is not re-exported from the package root.</p>"},{"location":"reference/Logger/#related-pages","title":"Related pages","text":"<ul> <li>MailReader</li> <li>Enumerations</li> <li>Exceptions</li> </ul>"},{"location":"reference/MailAddress/","title":"MailAddress","text":"<p><code>MailAddress</code> stores one mailbox with its display name and address components. Parsed message header properties expose mailboxes through <code>AddressList</code>.</p>"},{"location":"reference/MailAddress/#properties","title":"Properties","text":"Property Type Description <code>DisplayName</code> <code>str</code> Decoded human-readable name <code>Email</code> <code>str</code> Complete address specification <code>LocalPart</code> <code>str</code> Portion before <code>@</code> <code>Domain</code> <code>str</code> Portion after <code>@</code> <code>RawValue</code> <code>str</code> Source value supplied to <code>parse()</code> <code>IsInternationalized</code> <code>bool</code> True when the source contains non-ASCII characters <p>All properties default to an empty string except <code>IsInternationalized</code>, which defaults to <code>False</code>.</p>"},{"location":"reference/MailAddress/#parsing","title":"Parsing","text":"<pre><code>from EMLMailReader import MailAddress\n\naddress = MailAddress()\naddress.parse(\"Alice Example &lt;alice@example.com&gt;\")\n\nassert address.DisplayName == \"Alice Example\"\nassert address.Email == \"alice@example.com\"\nassert address.LocalPart == \"alice\"\nassert address.Domain == \"example.com\"\n</code></pre> <pre><code>parse(MailAddressString: str) -&gt; None\n</code></pre> <p><code>parse()</code> resets the instance, parses the first mailbox in the supplied value, and decodes an encoded display name. If structured parsing fails, it falls back to a simple mailbox/display-name split.</p> <p>For complete <code>To</code>, <code>From</code>, and similar header values, use <code>AddressList</code> so additional mailboxes and groups are not discarded.</p>"},{"location":"reference/MailAddress/#constructing-from-components","title":"Constructing from components","text":"<pre><code>address = MailAddress.from_parts(\n    display_name=\"Alice Example\",\n    username=\"alice\",\n    domain=\"example.com\",\n)\n</code></pre> <pre><code>from_parts(\n    display_name: str,\n    username: str,\n    domain: str,\n    raw_value: str = \"\",\n) -&gt; MailAddress\n</code></pre> <p>This class method is used by <code>AddressList</code> and is also available to callers.</p>"},{"location":"reference/MailAddress/#formatting-and-serialization","title":"Formatting and serialization","text":"<pre><code>header_value = address.to_header_value()\nsame_value = str(address)\ndata = address.to_dict()\n</code></pre> <p><code>to_header_value()</code> and <code>str(address)</code> use Python's header registry to quote or format the display name where possible.</p> <p><code>to_dict()</code> returns:</p> <pre><code>{\n    \"display_name\": \"Alice Example\",\n    \"email\": \"alice@example.com\",\n    \"local_part\": \"alice\",\n    \"domain\": \"example.com\",\n    \"raw_value\": \"Alice Example &lt;alice@example.com&gt;\",\n    \"internationalized\": False,\n}\n</code></pre>"},{"location":"reference/MailAddress/#related-pages","title":"Related pages","text":"<ul> <li>AddressList</li> <li>RxMailMessage</li> </ul>"},{"location":"reference/MailReader/","title":"MailReader","text":"<p><code>MailReader</code> is the public parsing fa\u00e7ade. It supports filesystem, bytes, string, and stream inputs and returns an <code>RxMailMessage</code> tree.</p>"},{"location":"reference/MailReader/#constructor","title":"Constructor","text":"<pre><code>MailReader(\n    logging_mode=LoggingMode.NONE,\n    TargetLoggingFolder=\"\",\n    parsing_mode=ParsingMode.MODERN,\n    limits=None,\n)\n</code></pre> Parameter Type Behavior <code>logging_mode</code> <code>LoggingMode</code> <code>NONE</code>, <code>CONSOLE</code>, or <code>FILE</code> <code>TargetLoggingFolder</code> <code>str</code> Existing output directory required for file logging <code>parsing_mode</code> <code>ParsingMode \\| str</code> <code>modern</code>, <code>lenient</code>, or <code>strict</code>; unknown strings fall back to modern <code>limits</code> <code>ParserLimits \\| None</code> Parser resource limits; defaults to <code>ParserLimits()</code> <p>The instance exposes <code>ParsingMode</code> and <code>LoggingMode</code> with the values supplied to the constructor.</p> <pre><code>from EMLMailReader import LoggingMode, MailReader, ParserLimits, ParsingMode\n\nreader = MailReader(\n    logging_mode=LoggingMode.CONSOLE,\n    parsing_mode=ParsingMode.MODERN,\n    limits=ParserLimits(max_message_bytes=25 * 1024 * 1024),\n)\n</code></pre>"},{"location":"reference/MailReader/#get_email","title":"<code>get_email()</code>","text":"<pre><code>get_email(emlPath: str) -&gt; RxMailMessage | None\n</code></pre> <p>Reads an EML file as bytes and delegates to <code>parse_bytes()</code>.</p> <ul> <li>Returns an <code>RxMailMessage</code> when parsing succeeds.</li> <li>Returns <code>None</code> when the path is missing, the file is empty, or a filesystem   read fails.</li> <li>Logs filesystem errors when logging is configured.</li> <li>Propagates <code>StandardsComplianceError</code> in strict mode.</li> <li>Closes the source file on success and failure.</li> </ul> <pre><code>message = MailReader().get_email(\"message.eml\")\nif message is None:\n    print(\"Message could not be read\")\n</code></pre> <p><code>FileMissingError</code> is used internally but is caught by <code>get_email()</code>; callers normally detect this case from the <code>None</code> result.</p>"},{"location":"reference/MailReader/#parse_bytes","title":"<code>parse_bytes()</code>","text":"<pre><code>parse_bytes(source: bytes) -&gt; RxMailMessage\n</code></pre> <p>This is the preferred in-memory entry point because the parser can retain the exact source octets in <code>RawSource</code>.</p> <pre><code>source = (\n    b\"Date: Fri, 21 Nov 1997 09:55:06 -0600\\r\\n\"\n    b\"From: Alice &lt;alice@example.com&gt;\\r\\n\"\n    b\"Subject: Hello\\r\\n\\r\\n\"\n    b\"Body\"\n)\nmessage = MailReader().parse_bytes(source)\n\nassert message.RawSource == source\nassert message.Subject == \"Hello\"\n</code></pre> <p>After parsing, diagnostics from the root and nested MIME parts are forwarded to the configured logger. The diagnostics remain attached to their message nodes.</p>"},{"location":"reference/MailReader/#parse_string","title":"<code>parse_string()</code>","text":"<pre><code>parse_string(source: str, encoding: str = \"utf-8\") -&gt; RxMailMessage\n</code></pre> <p>Encodes the string with the requested encoding, then calls <code>parse_bytes()</code>. Because this creates new bytes, use <code>parse_bytes()</code> when exact original wire bytes are available.</p> <pre><code>message = MailReader().parse_string(\n    \"Date: Fri, 21 Nov 1997 09:55:06 -0600\\r\\nFrom: alice@example.com\\r\\n\\r\\nHello\"\n)\n</code></pre>"},{"location":"reference/MailReader/#parse_stream","title":"<code>parse_stream()</code>","text":"<pre><code>parse_stream(stream) -&gt; RxMailMessage\n</code></pre> <p>Reads from the stream's current position. Binary stream data is passed through; text stream data is encoded as UTF-8.</p> <pre><code>from io import BytesIO, StringIO\n\nreader = MailReader()\nbinary_message = reader.parse_stream(BytesIO(source))\ntext_message = reader.parse_stream(StringIO(source.decode(\"ascii\")))\n</code></pre> <p>The caller owns the stream; <code>parse_stream()</code> does not close it.</p>"},{"location":"reference/MailReader/#parsing-modes","title":"Parsing modes","text":"<pre><code>from EMLMailReader import (\n    MailReader,\n    ParsingMode,\n    StandardsComplianceError,\n)\n\ntry:\n    message = MailReader(parsing_mode=ParsingMode.STRICT).parse_bytes(source)\nexcept StandardsComplianceError as error:\n    for diagnostic in error.diagnostics:\n        print(diagnostic.code)\n</code></pre> <ul> <li><code>MODERN</code> returns the parsed tree and diagnostics.</li> <li><code>LENIENT</code> is an explicit receiver-policy label and also returns the parsed   tree and diagnostics.</li> <li><code>STRICT</code> raises when any root or nested diagnostic has error severity.</li> </ul> <p>See Structured types for <code>ParserLimits</code>, <code>ParseDiagnostic</code>, and <code>StandardsComplianceError</code>.</p>"},{"location":"reference/MailReader/#logging","title":"Logging","text":"<pre><code>from EMLMailReader import LoggingMode, MailReader\n\nconsole_reader = MailReader(logging_mode=LoggingMode.CONSOLE)\nfile_reader = MailReader(\n    logging_mode=LoggingMode.FILE,\n    TargetLoggingFolder=\"/existing/log/directory\",\n)\n</code></pre> <p>File mode validates the target during construction and may raise <code>FolderNotAvailableError</code>. Configuring console or file logging replaces the process root logger configuration; see Logger.</p>"},{"location":"reference/MailReader/#related-pages","title":"Related pages","text":"<ul> <li>RxMailMessage</li> <li>Structured types</li> <li>Logger</li> <li>Exceptions</li> </ul>"},{"location":"reference/RxMailMessage/","title":"RxMailMessage","text":"<p><code>RxMailMessage</code> represents both the root Internet message and every MIME entity inside it. <code>Children</code> forms the MIME tree. Attachments and inline resources are views over those same nodes.</p> <p>Applications normally receive this class from <code>MailReader</code>; constructing it directly creates an empty message model.</p>"},{"location":"reference/RxMailMessage/#internet-message-fields","title":"Internet message fields","text":"Property Type Description <code>Headers</code> <code>HeaderCollection</code> Ordered, duplicate-preserving header occurrences <code>From</code> <code>AddressList</code> Originator mailbox/group list <code>Sender</code> <code>AddressList</code> Sender mailbox/group list <code>ReplyTo</code> <code>AddressList</code> Reply-To mailbox/group list <code>To</code> <code>AddressList</code> Primary recipients <code>Cc</code> <code>AddressList</code> Carbon-copy recipients <code>Bcc</code> <code>AddressList</code> Blind-copy recipients when present in the source <code>Subject</code> <code>str</code> Decoded subject value, or an empty string <code>Date</code> <code>ParsedDateTime \\| None</code> Raw and parsed date metadata <code>MessageID</code> <code>ParsedMessageID \\| None</code> Structured Message-ID <code>InReplyTo</code> <code>list[ParsedMessageID]</code> Parsed identifiers from In-Reply-To <code>References</code> <code>list[ParsedMessageID]</code> Parsed identifiers from References <code>Comments</code> <code>list[str]</code> All Comments header values <code>Keywords</code> <code>list[str]</code> All Keywords header values <code>Received</code> <code>list[str]</code> Unfolded Received values in source order <code>ReturnPath</code> <code>str</code> Unfolded Return-Path value <code>ResentBlocks</code> <code>list[ResentBlock]</code> Ordered groups of Resent fields <code>TraceBlocks</code> <code>list[TraceBlock]</code> Grouped Return-Path and Received fields <pre><code>message = MailReader().parse_bytes(source)\n\nfor mailbox in message.From.Mailboxes:\n    print(mailbox.DisplayName, mailbox.Email)\n\nfor received in message.Headers.get_all(\"Received\", decoded=False):\n    print(received)\n</code></pre>"},{"location":"reference/RxMailMessage/#mime-fields-and-source-data","title":"MIME fields and source data","text":"Property Type Description <code>ContentType</code> <code>ContentType</code> Effective media type plus explicit header metadata <code>ContentDisposition</code> <code>ContentDisposition \\| None</code> Parsed disposition when the header exists <code>ContentTransferEncoding</code> <code>TransferEncodingValue</code> Known or extension transfer-encoding token <code>MimeVersion</code> <code>str</code> MIME-Version header value <code>ContentDescription</code> <code>str</code> Decoded Content-Description <code>ContentID</code> <code>ParsedMessageID \\| None</code> Structured Content-ID <code>MessagePartial</code> <code>MessagePartialInfo \\| None</code> <code>message/partial</code> parameters <code>ExternalBodyAccess</code> <code>ExternalBodyAccessInfo \\| None</code> <code>message/external-body</code> parameters <code>RawSource</code> <code>bytes</code> Header and body bytes for this node <code>RawBody</code> <code>bytes</code> Body bytes before transfer decoding <code>DecodedBody</code> <code>bytes</code> Transfer-decoded payload for a leaf node <code>Children</code> <code>list[RxMailMessage]</code> Direct child MIME entities <code>Preamble</code> <code>str</code> Multipart preamble <code>Epilogue</code> <code>str</code> Multipart epilogue <code>Diagnostics</code> <code>list[ParseDiagnostic]</code> Findings attached to this node <p>For child nodes produced by Python's MIME parser, <code>RawSource</code> is a normalized serialization of that child. The root <code>RawSource</code> is the exact byte sequence supplied to <code>parse_bytes()</code> or read by <code>get_email()</code>.</p>"},{"location":"reference/RxMailMessage/#computed-properties","title":"Computed properties","text":""},{"location":"reference/RxMailMessage/#ismultipart","title":"<code>IsMultiPart</code>","text":"<p>True when the node has children or its media type starts with <code>multipart/</code>.</p>"},{"location":"reference/RxMailMessage/#body","title":"<code>Body</code>","text":"<ul> <li>A leaf returns its decoded text, or an empty string for a non-text leaf.</li> <li><code>multipart/alternative</code> returns the last non-empty child body.</li> <li>Other containers return the first non-empty child body.</li> </ul>"},{"location":"reference/RxMailMessage/#textbody-and-htmlbody","title":"<code>TextBody</code> and <code>HtmlBody</code>","text":"<p>These recursively return the first non-empty <code>text/plain</code> and <code>text/html</code> values, respectively.</p>"},{"location":"reference/RxMailMessage/#name","title":"<code>Name</code>","text":"<p>Uses <code>ContentDisposition.FileName</code> first, then <code>ContentType.Name</code>.</p>"},{"location":"reference/RxMailMessage/#isattachment-and-isinline","title":"<code>IsAttachment</code> and <code>IsInline</code>","text":"<p><code>IsInline</code> is true for an explicit <code>inline</code> disposition. <code>IsAttachment</code> is true for an explicit <code>attachment</code> disposition or a named part that is not inline.</p>"},{"location":"reference/RxMailMessage/#attachments-and-inlineresources","title":"<code>Attachments</code> and <code>InlineResources</code>","text":"<p>Recursive tuple views containing matching <code>RxMailMessage</code> nodes.</p> <pre><code>for attachment in message.Attachments:\n    print(\n        attachment.Name,\n        attachment.ContentType.MediaType,\n        len(attachment.DecodedBody),\n    )\n\nfor resource in message.InlineResources:\n    content_id = resource.ContentID.value if resource.ContentID else None\n    print(content_id, resource.Name)\n</code></pre>"},{"location":"reference/RxMailMessage/#mime-traversal","title":"MIME traversal","text":"<pre><code>def walk(part):\n    yield part\n    for child in part.Children:\n        yield from walk(child)\n\n\nfor part in walk(message):\n    print(part.ContentType.MediaType)\n</code></pre>"},{"location":"reference/RxMailMessage/#serialization","title":"Serialization","text":"<pre><code>data = message.to_dict()\njson_text = message.export_as_json()\n\nassert data[\"schema_version\"] == 2\n</code></pre> <p>Both methods emit the one canonical recursive schema. <code>export_as_json()</code> uses <code>ensure_ascii=False</code>. Raw and decoded byte payloads are not included in the JSON-compatible dictionary. Attachment and inline views are also not repeated; their parts already appear under <code>children</code>.</p>"},{"location":"reference/RxMailMessage/#saving-attachments","title":"Saving attachments","text":"<pre><code>message.save_attachments(\"/existing/output/directory\")\n</code></pre> <ul> <li>The directory must already exist or <code>FolderNotAvailableError</code> is raised.</li> <li>A supplied path is reduced to its basename.</li> <li>An empty, <code>.</code> or <code>..</code> name becomes <code>attachment</code>.</li> <li><code>DecodedBody</code> is written for normal attachment leaves.</li> <li>Existing files with the same name are overwritten.</li> <li>Multiple attachments with the same name target the same output file.</li> </ul>"},{"location":"reference/RxMailMessage/#related-pages","title":"Related pages","text":"<ul> <li>MailReader</li> <li>AddressList</li> <li>ContentType</li> <li>ContentDisposition</li> <li>Structured types</li> </ul>"},{"location":"reference/StructuredTypes/","title":"Structured types","text":"<p>The parser exports typed values for headers, diagnostics, identifiers, dates, resource limits, trace data, and MIME metadata. These types are available from the package root.</p>"},{"location":"reference/StructuredTypes/#parsingmode","title":"ParsingMode","text":"<pre><code>from EMLMailReader import ParsingMode\n</code></pre> Value Behavior <code>ParsingMode.MODERN</code> Return the parsed tree with diagnostics <code>ParsingMode.LENIENT</code> Explicit receiver-policy label; return the parsed tree with diagnostics <code>ParsingMode.STRICT</code> Raise <code>StandardsComplianceError</code> when any error diagnostic exists"},{"location":"reference/StructuredTypes/#parserlimits","title":"ParserLimits","text":"<p><code>ParserLimits</code> is a frozen data class passed to <code>MailReader</code>.</p> <pre><code>from EMLMailReader import MailReader, ParserLimits\n\nlimits = ParserLimits(\n    max_message_bytes=25 * 1024 * 1024,\n    max_header_bytes=512 * 1024,\n    max_header_count=5_000,\n    max_mime_depth=30,\n    max_parts=2_000,\n    max_decoded_part_bytes=10 * 1024 * 1024,\n)\nreader = MailReader(limits=limits)\n</code></pre> Field Default <code>max_message_bytes</code> 104,857,600 <code>max_header_bytes</code> 1,048,576 <code>max_header_count</code> 10,000 <code>max_mime_depth</code> 100 <code>max_parts</code> 10,000 <code>max_decoded_part_bytes</code> 104,857,600 <p>Limit violations produce error diagnostics. Strict mode raises after collecting the complete root and nested diagnostic set.</p>"},{"location":"reference/StructuredTypes/#diagnostics","title":"Diagnostics","text":""},{"location":"reference/StructuredTypes/#parsediagnostic","title":"ParseDiagnostic","text":"<pre><code>ParseDiagnostic(\n    code: str,\n    message: str,\n    severity: DiagnosticSeverity = DiagnosticSeverity.WARNING,\n    rfc: str | None = None,\n    section: str | None = None,\n    header: str | None = None,\n    line: int | None = None,\n    column: int | None = None,\n)\n</code></pre> <p><code>to_dict()</code> returns these fields and serializes severity as <code>\"info\"</code>, <code>\"warning\"</code>, or <code>\"error\"</code>.</p> <p>Diagnostics are stored on the affected <code>RxMailMessage.Diagnostics</code> list. Strict-mode exceptions expose a flattened tuple through <code>StandardsComplianceError.diagnostics</code>.</p> <pre><code>from EMLMailReader import DiagnosticSeverity\n\nerrors = [\n    item for item in message.Diagnostics if item.severity == DiagnosticSeverity.ERROR\n]\n</code></pre>"},{"location":"reference/StructuredTypes/#diagnosticseverity","title":"DiagnosticSeverity","text":"<ul> <li><code>INFO</code></li> <li><code>WARNING</code></li> <li><code>ERROR</code></li> </ul>"},{"location":"reference/StructuredTypes/#headercollection-and-headerfield","title":"HeaderCollection and HeaderField","text":"<p><code>HeaderCollection</code> is an ordered <code>Sequence[HeaderField]</code>. It preserves duplicates and performs case-insensitive lookup.</p> <pre><code>subject = message.Headers.get(\"subject\")\nreceived = message.Headers.get_all(\"Received\", decoded=False)\nsubject_occurrences = message.Headers.occurrences(\"Subject\")\n</code></pre> Method Result <code>get(name, default=None, decoded=True)</code> Last matching value or the default <code>get_all(name, decoded=True)</code> All matching values in source order <code>occurrences(name)</code> Matching <code>HeaderField</code> objects <code>append(field)</code> Add an occurrence <code>to_list()</code> Serialize every field <p>With <code>decoded=True</code>, lookups use <code>HeaderField.decoded_value</code>. With <code>decoded=False</code>, they use <code>unfolded_value</code>.</p> <p><code>HeaderField</code> is a frozen data class:</p> Field Type <code>name</code> <code>str</code> <code>raw_name</code> <code>str</code> <code>raw_value</code> <code>str</code> <code>unfolded_value</code> <code>str</code> <code>decoded_value</code> <code>str</code> <code>index</code> <code>int</code> <code>line</code> <code>int \\| None</code> <code>syntax_status</code> <code>SyntaxStatus</code> <p><code>SyntaxStatus</code> values are <code>CURRENT</code>, <code>OBSOLETE</code>, and <code>NONCONFORMANT</code>.</p>"},{"location":"reference/StructuredTypes/#parseddatetime","title":"ParsedDateTime","text":"<pre><code>ParsedDateTime(\n    raw_value: str,\n    value: datetime | None,\n    valid: bool,\n    obsolete: bool = False,\n)\n</code></pre> <p><code>to_dict()</code> serializes <code>value</code> with <code>datetime.isoformat()</code>. Invalid values keep their <code>raw_value</code>, set <code>value</code> to <code>None</code>, and set <code>valid</code> to false.</p>"},{"location":"reference/StructuredTypes/#parsedmessageid","title":"ParsedMessageID","text":"<pre><code>ParsedMessageID(\n    raw_value: str,\n    value: str,\n    left: str,\n    right: str,\n    valid: bool = True,\n)\n</code></pre> <p><code>raw_value</code> retains the original bracketed form. <code>value</code> is the identifier without angle brackets. Invalid source values still retain their parsed pieces with <code>valid=False</code>.</p>"},{"location":"reference/StructuredTypes/#transferencodingvalue","title":"TransferEncodingValue","text":"<pre><code>from EMLMailReader import TransferEncodingValue\n\nencoding = TransferEncodingValue.parse(\"x-custom\")\nassert encoding.raw_value == \"x-custom\"\nassert encoding.is_extension\n</code></pre> Field Type Description <code>raw_value</code> <code>str</code> Lowercase wire token <code>kind</code> <code>TransferEncoding</code> Known enum value or <code>UNKNOWN</code> <code>is_extension</code> <code>bool</code> True for an unrecognized/extension token <p><code>to_dict()</code> serializes <code>kind</code> using its string value.</p>"},{"location":"reference/StructuredTypes/#message-specific-mime-metadata","title":"Message-specific MIME metadata","text":""},{"location":"reference/StructuredTypes/#messagepartialinfo","title":"MessagePartialInfo","text":"<p>Used by <code>message/partial</code> nodes:</p> <pre><code>MessagePartialInfo(id: str, number: int | None, total: int | None)\n</code></pre>"},{"location":"reference/StructuredTypes/#externalbodyaccessinfo","title":"ExternalBodyAccessInfo","text":"<p>Used by <code>message/external-body</code> nodes:</p> <pre><code>ExternalBodyAccessInfo(access_type: str, parameters: dict)\n</code></pre>"},{"location":"reference/StructuredTypes/#resent-and-trace-blocks","title":"Resent and trace blocks","text":""},{"location":"reference/StructuredTypes/#resentblock","title":"ResentBlock","text":"<p>Contains a <code>fields: HeaderCollection</code>. <code>to_dict()</code> returns the serialized list of those header fields.</p>"},{"location":"reference/StructuredTypes/#traceblock","title":"TraceBlock","text":"<p>Contains:</p> <ul> <li><code>return_path: str | None</code></li> <li><code>received: list[str]</code></li> </ul> <p><code>to_dict()</code> returns both values without losing Received ordering.</p>"},{"location":"reference/StructuredTypes/#addressgroup","title":"AddressGroup","text":"<p>Address groups are documented with AddressList.</p>"},{"location":"reference/StructuredTypes/#related-pages","title":"Related pages","text":"<ul> <li>MailReader</li> <li>RxMailMessage</li> <li>Enumerations</li> <li>Exceptions</li> </ul>"},{"location":"reference/TextEncoding/","title":"TextEncoding","text":"<p><code>TextEncoding</code> provides four static decoding helpers. Parsed messages already use these decoding behaviors where appropriate; call the helpers directly when working with standalone encoded values.</p>"},{"location":"reference/TextEncoding/#decode_header","title":"<code>decode_header()</code>","text":"<pre><code>TextEncoding.decode_header(\n    encoded_string: str | None,\n    errors: str = \"replace\",\n) -&gt; str\n</code></pre> <p>Decodes and joins RFC 2047 encoded-word fragments.</p> <pre><code>from EMLMailReader import TextEncoding\n\nsubject = TextEncoding.decode_header(\"=?utf-8?b?SGVsbG8g4pyT?=\")\nassert subject == \"Hello \u2713\"\nassert TextEncoding.decode_header(None) == \"\"\n</code></pre> <p>Byte fragments use their declared charset, or ASCII when no charset is provided. The <code>errors</code> argument is passed to <code>bytes.decode()</code>.</p>"},{"location":"reference/TextEncoding/#decode_base64_string","title":"<code>decode_base64_string()</code>","text":"<pre><code>TextEncoding.decode_base64_string(\n    encoded_string: str,\n    string_charset: str = \"utf-8\",\n) -&gt; str\n</code></pre> <pre><code>text = TextEncoding.decode_base64_string(\"SGVsbG8=\")\nassert text == \"Hello\"\n</code></pre> <p>The method Base64-decodes the input and then decodes the resulting bytes with <code>string_charset</code>.</p>"},{"location":"reference/TextEncoding/#decode_base64_file","title":"<code>decode_base64_file()</code>","text":"<pre><code>TextEncoding.decode_base64_file(file_contents: str) -&gt; bytes\n</code></pre> <p>Despite its historical name, this method performs no file I/O. It accepts a Base64 string and returns decoded bytes.</p> <pre><code>payload = TextEncoding.decode_base64_file(\"AAEC\")\nassert payload == b\"\\x00\\x01\\x02\"\n</code></pre>"},{"location":"reference/TextEncoding/#decode_quoted_printable_string","title":"<code>decode_quoted_printable_string()</code>","text":"<pre><code>TextEncoding.decode_quoted_printable_string(\n    encoded_string: str,\n    string_charset: str,\n    is_header: bool,\n) -&gt; str\n</code></pre> <pre><code>text = TextEncoding.decode_quoted_printable_string(\n    \"caf=C3=A9\",\n    \"utf-8\",\n    False,\n)\nassert text == \"caf\u00e9\"\n</code></pre> <p>An empty charset is treated as UTF-8. <code>is_header=True</code> enables quoted-printable header rules, including underscore-to-space conversion.</p>"},{"location":"reference/TextEncoding/#error-behavior","title":"Error behavior","text":"<p>These are low-level helpers. Invalid Base64 data, unknown charsets, or undecodable bytes can raise standard Python decoding exceptions. They do not produce <code>ParseDiagnostic</code> objects when called directly.</p> <p>During normal message parsing, body charset failures are recovered with UTF-8 replacement decoding and a diagnostic is stored on the affected MIME node.</p>"},{"location":"reference/TextEncoding/#related-pages","title":"Related pages","text":"<ul> <li>MailReader</li> <li>RxMailMessage</li> <li>Structured types</li> </ul>"},{"location":"reference/generated-core/","title":"Generated core API","text":""},{"location":"reference/generated-core/#mailreader","title":"MailReader","text":""},{"location":"reference/generated-core/#EMLMailReader.Mail_Reader.MailReader","title":"EMLMailReader.Mail_Reader.MailReader","text":"<pre><code>MailReader(logging_mode: LoggingMode = LoggingMode.NONE, TargetLoggingFolder: str = '', parsing_mode: ParsingMode | str = ParsingMode.MODERN, limits: ParserLimits | None = None)\n</code></pre> <p>Configure and execute standards-aware EML parsing.</p> <p>A reader owns one parser configuration, including its receiver/strict mode, resource limits, and optional diagnostic logging destination.</p> <p>Attributes:</p> Name Type Description <code>ParsingMode</code> <p>Mode value supplied when the reader was created.</p> <code>LoggingMode</code> <p>Configured diagnostic logging destination.</p> <p>Initialize a reusable parser facade.</p> <p>Parameters:</p> Name Type Description Default <code>logging_mode</code> <code>LoggingMode</code> <p>Destination for parser diagnostic log messages.</p> <code>NONE</code> <code>TargetLoggingFolder</code> <code>str</code> <p>Existing directory used by file logging.</p> <code>''</code> <code>parsing_mode</code> <code>ParsingMode | str</code> <p>Receiver behavior or strict conformance enforcement.</p> <code>MODERN</code> <code>limits</code> <code>ParserLimits | None</code> <p>Resource guards; defaults to :class:<code>ParserLimits</code>.</p> <code>None</code> <p>Raises:</p> Type Description <code>FolderNotAvailableError</code> <p>If file logging is requested with an unavailable target directory.</p>"},{"location":"reference/generated-core/#EMLMailReader.Mail_Reader.MailReader.get_email","title":"get_email","text":"<pre><code>get_email(emlPath: str) -&gt; RxMailMessage | None\n</code></pre> <p>Read and parse an EML file.</p> <p>Parameters:</p> Name Type Description Default <code>emlPath</code> <code>str</code> <p>Path to the source EML file.</p> required <p>Returns:</p> Type Description <code>RxMailMessage | None</code> <p>The parsed message, or <code>None</code> when the file is missing, empty, or</p> <code>RxMailMessage | None</code> <p>cannot be read.</p> <p>Raises:</p> Type Description <code>StandardsComplianceError</code> <p>If strict parsing finds an error-level diagnostic.</p>"},{"location":"reference/generated-core/#EMLMailReader.Mail_Reader.MailReader.parse_bytes","title":"parse_bytes","text":"<pre><code>parse_bytes(source: bytes) -&gt; RxMailMessage\n</code></pre> <p>Parse wire bytes while preserving the exact root source octets.</p> <p>Parser diagnostics remain attached to their MIME nodes and are also forwarded to the configured logger.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>bytes</code> <p>Complete Internet message bytes.</p> required <p>Returns:</p> Type Description <code>RxMailMessage</code> <p>The root :class:<code>RxMailMessage</code>.</p> <p>Raises:</p> Type Description <code>StandardsComplianceError</code> <p>If strict parsing finds an error-level diagnostic.</p>"},{"location":"reference/generated-core/#EMLMailReader.Mail_Reader.MailReader.parse_string","title":"parse_string","text":"<pre><code>parse_string(source: str, encoding: str = 'utf-8') -&gt; RxMailMessage\n</code></pre> <p>Encode and parse an in-memory Internet message string.</p> <p>Parameters:</p> Name Type Description Default <code>source</code> <code>str</code> <p>Complete message text.</p> required <code>encoding</code> <code>str</code> <p>Encoding used to create parser input bytes.</p> <code>'utf-8'</code> <p>Returns:</p> Type Description <code>RxMailMessage</code> <p>The parsed root message.</p>"},{"location":"reference/generated-core/#EMLMailReader.Mail_Reader.MailReader.parse_stream","title":"parse_stream","text":"<pre><code>parse_stream(stream: IO[bytes] | IO[str]) -&gt; RxMailMessage\n</code></pre> <p>Read and parse a binary or text stream from its current position.</p> <p>Text streams are encoded as UTF-8. The caller retains ownership of the stream and is responsible for closing it.</p> <p>Parameters:</p> Name Type Description Default <code>stream</code> <code>IO[bytes] | IO[str]</code> <p>File-like object whose <code>read()</code> returns bytes or text.</p> required <p>Returns:</p> Type Description <code>RxMailMessage</code> <p>The parsed root message.</p>"},{"location":"reference/generated-core/#rxmailmessage","title":"RxMailMessage","text":""},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage","title":"EMLMailReader.Rx_Mail_Message.RxMailMessage","text":"<pre><code>RxMailMessage()\n</code></pre> <p>Represent one parsed Internet message or MIME entity.</p> <p>The same class is used for the root message and every node in its recursive MIME tree. Internet-message fields, MIME metadata, source bytes, decoded bodies, child entities, and parser diagnostics therefore remain available through one canonical model.</p> <p>Attributes:</p> Name Type Description <code>Headers</code> <p>Ordered, duplicate-preserving header collection.</p> <code>From</code> <p>Structured originator address list.</p> <code>Sender</code> <p>Structured sender address list.</p> <code>ReplyTo</code> <p>Structured Reply-To address list.</p> <code>To</code> <p>Structured primary-recipient address list.</p> <code>Cc</code> <p>Structured carbon-copy address list.</p> <code>Bcc</code> <p>Structured blind-copy address list.</p> <code>Subject</code> <p>Decoded Subject field value.</p> <code>Date</code> <code>ParsedDateTime | None</code> <p>Structured parsed Date value or <code>None</code>.</p> <code>MessageID</code> <code>ParsedMessageID | None</code> <p>Structured Message-ID value or <code>None</code>.</p> <code>InReplyTo</code> <code>list[ParsedMessageID]</code> <p>Parsed identifiers from the In-Reply-To field.</p> <code>References</code> <code>list[ParsedMessageID]</code> <p>Parsed identifiers from the References field.</p> <code>Comments</code> <code>list[str]</code> <p>Decoded Comments field values in source order.</p> <code>Keywords</code> <code>list[str]</code> <p>Decoded Keywords field values in source order.</p> <code>Received</code> <code>list[str]</code> <p>Unfolded Received values in source order.</p> <code>ReturnPath</code> <p>Unfolded Return-Path value.</p> <code>ResentBlocks</code> <code>list[ResentBlock]</code> <p>Contiguous structured Resent field groups.</p> <code>TraceBlocks</code> <code>list[TraceBlock]</code> <p>Structured Return-Path and Received groups.</p> <code>ContentType</code> <p>Effective and source MIME content type.</p> <code>ContentDisposition</code> <code>ContentDisposition | None</code> <p>Parsed disposition or <code>None</code>.</p> <code>ContentTransferEncoding</code> <p>Lossless transfer-encoding value.</p> <code>MimeVersion</code> <p>Unfolded MIME-Version field value.</p> <code>ContentDescription</code> <p>Decoded Content-Description value.</p> <code>ContentID</code> <code>ParsedMessageID | None</code> <p>Structured Content-ID value or <code>None</code>.</p> <code>MessagePartial</code> <code>MessagePartialInfo | None</code> <p><code>message/partial</code> metadata or <code>None</code>.</p> <code>ExternalBodyAccess</code> <code>ExternalBodyAccessInfo | None</code> <p><code>message/external-body</code> metadata or <code>None</code>.</p> <code>RawSource</code> <p>Source bytes for this entity; exact input bytes at the root.</p> <code>RawBody</code> <p>Body bytes before content-transfer decoding.</p> <code>DecodedBody</code> <p>Transfer-decoded leaf payload bytes.</p> <code>Children</code> <code>list[RxMailMessage]</code> <p>Direct child MIME entities.</p> <code>Preamble</code> <p>Text preceding the first multipart boundary.</p> <code>Epilogue</code> <p>Text following the closing multipart boundary.</p> <code>Diagnostics</code> <code>list[ParseDiagnostic]</code> <p>Standards findings attached to this entity.</p> <p>Initialize an empty message using RFC/MIME default field values.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.IsMultiPart","title":"IsMultiPart  <code>property</code>","text":"<pre><code>IsMultiPart: bool\n</code></pre> <p>Return whether the entity is a multipart container or has children.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.Body","title":"Body  <code>property</code>","text":"<pre><code>Body: str\n</code></pre> <p>Return the preferred decoded text for this MIME subtree.</p> <p>Leaves return their decoded text. <code>multipart/alternative</code> chooses its last non-empty alternative; other containers choose the first non-empty child body.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.TextBody","title":"TextBody  <code>property</code>","text":"<pre><code>TextBody: str\n</code></pre> <p>Return the first decoded <code>text/plain</code> body in this subtree.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.HtmlBody","title":"HtmlBody  <code>property</code>","text":"<pre><code>HtmlBody: str\n</code></pre> <p>Return the first decoded <code>text/html</code> body in this subtree.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.Name","title":"Name  <code>property</code>","text":"<pre><code>Name: str\n</code></pre> <p>Return the disposition filename or fallback Content-Type name.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.IsInline","title":"IsInline  <code>property</code>","text":"<pre><code>IsInline: bool\n</code></pre> <p>Return whether the entity has an explicit inline disposition.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.IsAttachment","title":"IsAttachment  <code>property</code>","text":"<pre><code>IsAttachment: bool\n</code></pre> <p>Return whether the entity should be exposed as an attachment.</p> <p>Explicit attachment dispositions qualify. A named entity also qualifies unless it is explicitly inline.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.Attachments","title":"Attachments  <code>property</code>","text":"<pre><code>Attachments: tuple[RxMailMessage, ...]\n</code></pre> <p>Return attachment entities in this subtree as an immutable tuple.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.InlineResources","title":"InlineResources  <code>property</code>","text":"<pre><code>InlineResources: tuple[RxMailMessage, ...]\n</code></pre> <p>Return explicitly inline entities in this subtree as an immutable tuple.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.export_as_json","title":"export_as_json","text":"<pre><code>export_as_json() -&gt; str\n</code></pre> <p>Serialize the canonical recursive schema as Unicode-preserving JSON.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return the complete JSON-compatible canonical message schema.</p> <p>Source and decoded byte payloads are intentionally omitted. Attachment and inline-resource views are not duplicated because those entities already appear recursively under <code>children</code>.</p>"},{"location":"reference/generated-core/#EMLMailReader.Rx_Mail_Message.RxMailMessage.save_attachments","title":"save_attachments","text":"<pre><code>save_attachments(TargetFolderPath: str) -&gt; None\n</code></pre> <p>Write every attachment payload in this subtree to a directory.</p> <p>Filenames are reduced to their basename; empty or unsafe basename-only values use <code>attachment</code>. Existing files with the same name are overwritten.</p> <p>Parameters:</p> Name Type Description Default <code>TargetFolderPath</code> <code>str</code> <p>Existing destination directory.</p> required <p>Raises:</p> Type Description <code>FolderNotAvailableError</code> <p>If the destination directory is missing.</p>"},{"location":"reference/generated-mime/","title":"Generated MIME API","text":""},{"location":"reference/generated-mime/#contenttype","title":"ContentType","text":""},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType","title":"EMLMailReader.Content_Type.ContentType","text":"<pre><code>ContentType()\n</code></pre> <p>Represent the effective and source MIME content type for one entity.</p> <p>The class preserves whether Content-Type was explicit, retains all decoded parameters, and exposes frequently used MIME values as dedicated attributes.</p> <p>Attributes:</p> Name Type Description <code>MediaType</code> <p>Effective lowercase <code>type/subtype</code> value.</p> <code>Charset</code> <p>Declared text charset, defaulting to MIME US-ASCII.</p> <code>Boundary</code> <p>Multipart boundary or an empty string.</p> <code>Name</code> <p>Suggested content name from the <code>name</code> parameter.</p> <code>Parameters</code> <code>dict[str, str]</code> <p>All decoded parameters keyed by lowercase name.</p> <code>RawValue</code> <p>Original Content-Type field value, excluding the field name.</p> <code>IsExplicit</code> <p>Whether the source contained a Content-Type field.</p> <p>Initialize the RFC 2045 default <code>text/plain; charset=us-ascii</code>.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.MediaType","title":"MediaType  <code>instance-attribute</code>","text":"<pre><code>MediaType = 'text/plain'\n</code></pre> <p>The main media type and subtype (e.g., 'text/plain', 'image/jpeg').</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.Charset","title":"Charset  <code>instance-attribute</code>","text":"<pre><code>Charset = 'us-ascii'\n</code></pre> <p>Character encoding used for text content (defaults to US-ASCII per RFC 2045).</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.Boundary","title":"Boundary  <code>instance-attribute</code>","text":"<pre><code>Boundary = ''\n</code></pre> <p>Delimiter string used to separate parts in multipart MIME entities.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.Name","title":"Name  <code>instance-attribute</code>","text":"<pre><code>Name = ''\n</code></pre> <p>Suggested name for the content, often used for attachments.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.Parameters","title":"Parameters  <code>instance-attribute</code>","text":"<pre><code>Parameters: dict[str, str] = {}\n</code></pre> <p>All decoded MIME parameters, including RFC 2231 extensions.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.parse","title":"parse","text":"<pre><code>parse(ContentTypeString: str | None = None, *, effective_media_type: str | None = None) -&gt; None\n</code></pre> <p>Parse a Content-Type value and replace this instance's metadata.</p> <p>Parameters:</p> Name Type Description Default <code>ContentTypeString</code> <code>str | None</code> <p>Source field value, or <code>None</code> when omitted.</p> <code>None</code> <code>effective_media_type</code> <code>str | None</code> <p>Media type inferred by the MIME parser when no explicit field exists. Defaults to <code>text/plain</code>.</p> <code>None</code>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.get_parameter","title":"get_parameter","text":"<pre><code>get_parameter(name: str, default: _DefaultT | None = None) -&gt; str | _DefaultT | None\n</code></pre> <p>Return a decoded parameter by case-insensitive name.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.to_header_value","title":"to_header_value","text":"<pre><code>to_header_value() -&gt; str\n</code></pre> <p>Return the normalized Content-Type value including its parameters.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Type.ContentType.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible representation of the content type.</p>"},{"location":"reference/generated-mime/#contentdisposition","title":"ContentDisposition","text":""},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition","title":"EMLMailReader.Content_Disposition.ContentDisposition","text":"<pre><code>ContentDisposition()\n</code></pre> <p>Represent a parsed MIME Content-Disposition field.</p> <p>The model retains the original field value and every decoded parameter while exposing common RFC 2183 metadata such as the filename, size, and lifecycle dates through dedicated attributes.</p> <p>Attributes:</p> Name Type Description <code>DispositionType</code> <p>Lowercase registered or extension disposition token.</p> <code>FileName</code> <p>Suggested filename decoded from the <code>filename</code> parameter.</p> <code>CreationDate</code> <code>ParsedDateTime | None</code> <p>Parsed <code>creation-date</code> value, if present.</p> <code>ModificationDate</code> <code>ParsedDateTime | None</code> <p>Parsed <code>modification-date</code> value, if present.</p> <code>ReadDate</code> <code>ParsedDateTime | None</code> <p>Parsed <code>read-date</code> value, if present.</p> <code>Size</code> <p>Declared content size in bytes, or zero when absent or invalid.</p> <code>Parameters</code> <code>dict[str, str]</code> <p>All decoded parameters keyed by lowercase name.</p> <code>RawValue</code> <p>Original Content-Disposition field value.</p> <code>IsExplicit</code> <p>Whether the source contained a Content-Disposition field.</p> <p>Initialize an empty, non-explicit disposition value.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.DispositionType","title":"DispositionType  <code>instance-attribute</code>","text":"<pre><code>DispositionType = ''\n</code></pre> <p>The registered or extension disposition token.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.FileName","title":"FileName  <code>instance-attribute</code>","text":"<pre><code>FileName = ''\n</code></pre> <p>The suggested filename for the MIME entity when saved to disk.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.CreationDate","title":"CreationDate  <code>instance-attribute</code>","text":"<pre><code>CreationDate: ParsedDateTime | None = None\n</code></pre> <p>RFC 2822 formatted date when the MIME entity was originally created.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.ModificationDate","title":"ModificationDate  <code>instance-attribute</code>","text":"<pre><code>ModificationDate: ParsedDateTime | None = None\n</code></pre> <p>RFC 2822 formatted date when the MIME entity was last modified.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.Size","title":"Size  <code>instance-attribute</code>","text":"<pre><code>Size = 0\n</code></pre> <p>Size of the MIME entity content in bytes.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.parse","title":"parse","text":"<pre><code>parse(ContentDispositionString: str) -&gt; None\n</code></pre> <p>Parse a field value and replace this instance's disposition metadata.</p> <p>Parameters:</p> Name Type Description Default <code>ContentDispositionString</code> <code>str</code> <p>Content-Disposition value without the field name.</p> required"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.get_parameter","title":"get_parameter","text":"<pre><code>get_parameter(name: str, default: _DefaultT | None = None) -&gt; str | _DefaultT | None\n</code></pre> <p>Return a decoded parameter by case-insensitive name.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Parameter name to look up.</p> required <code>default</code> <code>_DefaultT | None</code> <p>Value returned when the parameter is absent.</p> <code>None</code>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.to_header_value","title":"to_header_value","text":"<pre><code>to_header_value() -&gt; str\n</code></pre> <p>Return the normalized Content-Disposition value for a header field.</p>"},{"location":"reference/generated-mime/#EMLMailReader.Content_Disposition.ContentDisposition.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible representation of the disposition metadata.</p>"},{"location":"reference/generated-standards/","title":"Generated structured API","text":""},{"location":"reference/generated-standards/#addresses","title":"Addresses","text":""},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.MailAddress","title":"EMLMailReader.Mail_Address.MailAddress","text":"<pre><code>MailAddress()\n</code></pre> <p>Represent one mailbox from an RFC 5322 or RFC 6532 address field.</p> <p>Attributes:</p> Name Type Description <code>DisplayName</code> <p>Decoded human-readable phrase preceding the address.</p> <code>Email</code> <p>Complete address specification.</p> <code>LocalPart</code> <p>Portion of the address before <code>@</code>.</p> <code>Domain</code> <p>Portion of the address after <code>@</code>.</p> <code>RawValue</code> <p>Original mailbox value when available.</p> <code>IsInternationalized</code> <p>Whether the source contains non-ASCII characters.</p> <p>Initialize an empty mailbox.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.MailAddress.DisplayName","title":"DisplayName  <code>instance-attribute</code>","text":"<pre><code>DisplayName = ''\n</code></pre> <p>The human-readable name associated with the email address (optional).</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.MailAddress.Email","title":"Email  <code>instance-attribute</code>","text":"<pre><code>Email = ''\n</code></pre> <p>The actual email address (user@domain.com format).</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.MailAddress.parse","title":"parse","text":"<pre><code>parse(MailAddressString: str) -&gt; None\n</code></pre> <p>Parse the first mailbox from a source address value.</p> <p>The instance is reset before parsing. If the standard header parser rejects the value, a conservative fallback retains as much display-name and address text as possible.</p> <p>Parameters:</p> Name Type Description Default <code>MailAddressString</code> <code>str</code> <p>Mailbox value such as <code>\"Jane Doe &lt;jane@example.com&gt;\"</code>.</p> required"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.MailAddress.from_parts","title":"from_parts  <code>classmethod</code>","text":"<pre><code>from_parts(display_name: str, username: str, domain: str, raw_value: str = '') -&gt; MailAddress\n</code></pre> <p>Build a mailbox from components produced by the header registry.</p> <p>Parameters:</p> Name Type Description Default <code>display_name</code> <code>str</code> <p>Decoded display phrase.</p> required <code>username</code> <code>str</code> <p>Local part of the address.</p> required <code>domain</code> <code>str</code> <p>Domain part of the address.</p> required <code>raw_value</code> <code>str</code> <p>Optional source representation to retain.</p> <code>''</code> <p>Returns:</p> Type Description <code>MailAddress</code> <p>A populated :class:<code>MailAddress</code>.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.MailAddress.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible mailbox representation.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.MailAddress.to_header_value","title":"to_header_value","text":"<pre><code>to_header_value() -&gt; str\n</code></pre> <p>Return a normalized mailbox value suitable for an address header.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.AddressList","title":"EMLMailReader.Mail_Address.AddressList","text":"<pre><code>AddressList(raw_value: str = '')\n</code></pre> <p>Represent a mailbox list while retaining named and empty groups.</p> <p>Attributes:</p> Name Type Description <code>RawValue</code> <p>Original complete address-list value.</p> <code>Items</code> <code>list[MailAddress | AddressGroup]</code> <p>Ordered top-level :class:<code>MailAddress</code> and :class:<code>AddressGroup</code> values.</p> <p>Initialize an address list and optionally parse a source value.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.AddressList.Mailboxes","title":"Mailboxes  <code>property</code>","text":"<pre><code>Mailboxes: tuple[MailAddress, ...]\n</code></pre> <p>Return all direct and grouped mailboxes as an immutable flat tuple.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.AddressList.parse","title":"parse","text":"<pre><code>parse(value: str) -&gt; AddressList\n</code></pre> <p>Parse an RFC 5322/6854 address list in place.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>str</code> <p>Complete address-list value without the header name.</p> required <p>Returns:</p> Type Description <code>AddressList</code> <p>This instance, allowing fluent construction.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.AddressList.to_header_value","title":"to_header_value","text":"<pre><code>to_header_value() -&gt; str\n</code></pre> <p>Return a normalized address-list value while preserving groups.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Mail_Address.AddressList.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; list[JsonObject]\n</code></pre> <p>Return ordered JSON-compatible mailbox and group records.</p>"},{"location":"reference/generated-standards/#standards-values","title":"Standards values","text":""},{"location":"reference/generated-standards/#EMLMailReader.Standards","title":"EMLMailReader.Standards","text":"<p>Standards, diagnostics, limits, and lossless structured parser values.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParsingMode","title":"ParsingMode","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Control how parser diagnostics affect the result.</p> <p><code>MODERN</code> and <code>LENIENT</code> return recoverable messages with diagnostics. <code>STRICT</code> raises when any error-level diagnostic is collected.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.SyntaxStatus","title":"SyntaxStatus","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Classify header syntax as current, accepted obsolete, or invalid.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.DiagnosticSeverity","title":"DiagnosticSeverity","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Classify a finding as informational, recoverable, or error-level.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.StandardsComplianceError","title":"StandardsComplianceError","text":"<pre><code>StandardsComplianceError(diagnostics: Iterable[ParseDiagnostic])\n</code></pre> <p>               Bases: <code>ValueError</code></p> <p>Report strict-mode failure with the complete diagnostic collection.</p> <p>Attributes:</p> Name Type Description <code>diagnostics</code> <code>tuple[ParseDiagnostic, ...]</code> <p>Immutable root and nested diagnostics collected before the exception was raised.</p> <p>Create an exception from an iterable of parser diagnostics.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParserLimits","title":"ParserLimits  <code>dataclass</code>","text":"<pre><code>ParserLimits(max_message_bytes: int = 100 * 1024 * 1024, max_header_bytes: int = 1024 * 1024, max_header_count: int = 10000, max_mime_depth: int = 100, max_parts: int = 10000, max_decoded_part_bytes: int = 100 * 1024 * 1024)\n</code></pre> <p>Set resource guards for hostile or accidentally enormous messages.</p> <p>Attributes:</p> Name Type Description <code>max_message_bytes</code> <code>int</code> <p>Maximum complete source size.</p> <code>max_header_bytes</code> <code>int</code> <p>Maximum root header-section size.</p> <code>max_header_count</code> <code>int</code> <p>Maximum root header occurrence count.</p> <code>max_mime_depth</code> <code>int</code> <p>Maximum recursive MIME depth.</p> <code>max_parts</code> <code>int</code> <p>Maximum MIME entity count, including the root.</p> <code>max_decoded_part_bytes</code> <code>int</code> <p>Maximum transfer-decoded size per leaf.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParseDiagnostic","title":"ParseDiagnostic  <code>dataclass</code>","text":"<pre><code>ParseDiagnostic(code: str, message: str, severity: DiagnosticSeverity = DiagnosticSeverity.WARNING, rfc: str | None = None, section: str | None = None, header: str | None = None, line: int | None = None, column: int | None = None)\n</code></pre> <p>Describe one standards, syntax, decoding, or resource-limit finding.</p> <p>Attributes:</p> Name Type Description <code>code</code> <code>str</code> <p>Stable machine-readable diagnostic identifier.</p> <code>message</code> <code>str</code> <p>Human-readable explanation.</p> <code>severity</code> <code>DiagnosticSeverity</code> <p>Informational, warning, or error impact.</p> <code>rfc</code> <code>str | None</code> <p>Relevant RFC label when known.</p> <code>section</code> <code>str | None</code> <p>Relevant standards section when known.</p> <code>header</code> <code>str | None</code> <p>Associated header field name when applicable.</p> <code>line</code> <code>int | None</code> <p>One-based source line when known.</p> <code>column</code> <code>int | None</code> <p>One-based source column when known.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParseDiagnostic.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible diagnostic record.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderField","title":"HeaderField  <code>dataclass</code>","text":"<pre><code>HeaderField(name: str, raw_name: str, raw_value: str, unfolded_value: str, decoded_value: str, index: int, line: int | None = None, syntax_status: SyntaxStatus = SyntaxStatus.CURRENT)\n</code></pre> <p>Represent one header occurrence without losing source information.</p> <p>Attributes:</p> Name Type Description <code>name</code> <code>str</code> <p>Lookup name for the field.</p> <code>raw_name</code> <code>str</code> <p>Field name as it appeared in the source.</p> <code>raw_value</code> <code>str</code> <p>Folded source value.</p> <code>unfolded_value</code> <code>str</code> <p>Source value with legal folding removed.</p> <code>decoded_value</code> <code>str</code> <p>Interpreted Unicode value.</p> <code>index</code> <code>int</code> <p>Zero-based position in the header block.</p> <code>line</code> <code>int | None</code> <p>One-based source line when available.</p> <code>syntax_status</code> <code>SyntaxStatus</code> <p>Current, obsolete, or nonconformant syntax classification.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderField.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible header occurrence.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderCollection","title":"HeaderCollection","text":"<pre><code>HeaderCollection(fields: Iterable[HeaderField] | None = None)\n</code></pre> <p>               Bases: <code>Sequence[HeaderField]</code></p> <p>Store ordered duplicate headers with case-insensitive lookup.</p> <p>Initialize the collection from optional header occurrences.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderCollection.append","title":"append","text":"<pre><code>append(value: HeaderField) -&gt; None\n</code></pre> <p>Append a header occurrence without deduplicating its name.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderCollection.get","title":"get","text":"<pre><code>get(name: str, default: None = None, *, decoded: bool = True) -&gt; str | None\n</code></pre><pre><code>get(name: str, default: _DefaultT, *, decoded: bool = True) -&gt; str | _DefaultT\n</code></pre> <pre><code>get(name: str, default: _DefaultT | None = None, *, decoded: bool = True) -&gt; str | _DefaultT | None\n</code></pre> <p>Return the last matching value or a default.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Case-insensitive field name.</p> required <code>default</code> <code>_DefaultT | None</code> <p>Value returned when no occurrence exists.</p> <code>None</code> <code>decoded</code> <code>bool</code> <p>Select decoded text instead of unfolded source text.</p> <code>True</code>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderCollection.get_all","title":"get_all","text":"<pre><code>get_all(name: str, *, decoded: bool = True) -&gt; list[str]\n</code></pre> <p>Return all matching values in source order.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderCollection.occurrences","title":"occurrences","text":"<pre><code>occurrences(name: str) -&gt; list[HeaderField]\n</code></pre> <p>Return all matching :class:<code>HeaderField</code> objects in source order.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.HeaderCollection.to_list","title":"to_list","text":"<pre><code>to_list() -&gt; list[JsonObject]\n</code></pre> <p>Serialize every occurrence in source order.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.TransferEncodingValue","title":"TransferEncodingValue  <code>dataclass</code>","text":"<pre><code>TransferEncodingValue(raw_value: str, kind: TransferEncoding, is_extension: bool = False)\n</code></pre> <p>Represent a known or extension Content-Transfer-Encoding token.</p> <p>Attributes:</p> Name Type Description <code>raw_value</code> <code>str</code> <p>Normalized lowercase token from the field.</p> <code>kind</code> <code>TransferEncoding</code> <p>Matching known :class:<code>TransferEncoding</code> or <code>UNKNOWN</code>.</p> <code>is_extension</code> <code>bool</code> <p>Whether the token is not one of the standard encodings.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.TransferEncodingValue.parse","title":"parse  <code>classmethod</code>","text":"<pre><code>parse(value: str | None) -&gt; TransferEncodingValue\n</code></pre> <p>Classify a transfer-encoding token without discarding extensions.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.TransferEncodingValue.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible transfer-encoding record.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.AddressGroup","title":"AddressGroup  <code>dataclass</code>","text":"<pre><code>AddressGroup(display_name: str, addresses: tuple[MailAddress, ...], raw_value: str = '')\n</code></pre> <p>Represent a named or empty address group.</p> <p>Attributes:</p> Name Type Description <code>display_name</code> <code>str</code> <p>Group phrase preceding the colon.</p> <code>addresses</code> <code>tuple[MailAddress, ...]</code> <p>Ordered member mailboxes.</p> <code>raw_value</code> <code>str</code> <p>Source address-list value retained for diagnostics or display.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.AddressGroup.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible group and its member mailboxes.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParsedMessageID","title":"ParsedMessageID  <code>dataclass</code>","text":"<pre><code>ParsedMessageID(raw_value: str, value: str, left: str, right: str, valid: bool = True)\n</code></pre> <p>Retain raw and decomposed forms of an Internet message identifier.</p> <p>Attributes:</p> Name Type Description <code>raw_value</code> <code>str</code> <p>Source value, normally including angle brackets.</p> <code>value</code> <code>str</code> <p>Identifier without surrounding angle brackets.</p> <code>left</code> <code>str</code> <p>Identifier portion before <code>@</code>.</p> <code>right</code> <code>str</code> <p>Identifier portion after <code>@</code>.</p> <code>valid</code> <code>bool</code> <p>Whether the source matched the supported msg-id syntax.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParsedMessageID.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible message-identifier record.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParsedDateTime","title":"ParsedDateTime  <code>dataclass</code>","text":"<pre><code>ParsedDateTime(raw_value: str, value: datetime | None, valid: bool, obsolete: bool = False)\n</code></pre> <p>Retain a source date together with its parsed Python value.</p> <p>Attributes:</p> Name Type Description <code>raw_value</code> <code>str</code> <p>Original date-time field value.</p> <code>value</code> <code>datetime | None</code> <p>Parsed timezone-aware or naive datetime, or <code>None</code>.</p> <code>valid</code> <code>bool</code> <p>Whether parsing succeeded.</p> <code>obsolete</code> <code>bool</code> <p>Whether accepted obsolete date syntax was detected.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ParsedDateTime.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible date record using ISO 8601 text.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.MessagePartialInfo","title":"MessagePartialInfo  <code>dataclass</code>","text":"<pre><code>MessagePartialInfo(id: str, number: int | None, total: int | None)\n</code></pre> <p>Represent RFC 2046 <code>message/partial</code> reassembly metadata.</p> <p>Attributes:</p> Name Type Description <code>id</code> <code>str</code> <p>Identifier shared by all fragments.</p> <code>number</code> <code>int | None</code> <p>One-based fragment number.</p> <code>total</code> <code>int | None</code> <p>Declared total fragment count when present.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.MessagePartialInfo.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible fragment metadata record.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ExternalBodyAccessInfo","title":"ExternalBodyAccessInfo  <code>dataclass</code>","text":"<pre><code>ExternalBodyAccessInfo(access_type: str, parameters: dict[str, str])\n</code></pre> <p>Represent RFC 2046 <code>message/external-body</code> access metadata.</p> <p>Attributes:</p> Name Type Description <code>access_type</code> <code>str</code> <p>Mechanism used to retrieve the external body.</p> <code>parameters</code> <code>dict[str, str]</code> <p>Complete decoded Content-Type parameter mapping.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ExternalBodyAccessInfo.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible external-body metadata record.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ResentBlock","title":"ResentBlock  <code>dataclass</code>","text":"<pre><code>ResentBlock(fields: HeaderCollection = HeaderCollection())\n</code></pre> <p>Group one contiguous set of RFC 5322 Resent fields.</p> <p>Attributes:</p> Name Type Description <code>fields</code> <code>HeaderCollection</code> <p>Ordered header occurrences belonging to the block.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.ResentBlock.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; list[JsonObject]\n</code></pre> <p>Serialize the block as its ordered header-field records.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.TraceBlock","title":"TraceBlock  <code>dataclass</code>","text":"<pre><code>TraceBlock(return_path: str | None = None, received: list[str] = list())\n</code></pre> <p>Group one Return-Path with its following Received trace fields.</p> <p>Attributes:</p> Name Type Description <code>return_path</code> <code>str | None</code> <p>Unfolded Return-Path value when present.</p> <code>received</code> <code>list[str]</code> <p>Unfolded Received values in source order.</p>"},{"location":"reference/generated-standards/#EMLMailReader.Standards.TraceBlock.to_dict","title":"to_dict","text":"<pre><code>to_dict() -&gt; JsonObject\n</code></pre> <p>Return a JSON-compatible trace block.</p>"},{"location":"reference/generated-utilities/","title":"Generated utility API","text":""},{"location":"reference/generated-utilities/#textencoding","title":"TextEncoding","text":""},{"location":"reference/generated-utilities/#EMLMailReader.Text_Encoding.TextEncoding","title":"EMLMailReader.Text_Encoding.TextEncoding","text":"<p>Decode common MIME transfer encodings and RFC 2047 header text.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Text_Encoding.TextEncoding.decode_quoted_printable_string","title":"decode_quoted_printable_string  <code>staticmethod</code>","text":"<pre><code>decode_quoted_printable_string(encoded_string: str, string_charset: str, is_header: bool) -&gt; str\n</code></pre> <p>Decode quoted-printable text using the supplied character set.</p> <p>Parameters:</p> Name Type Description Default <code>encoded_string</code> <code>str</code> <p>Quoted-printable source text.</p> required <code>string_charset</code> <code>str</code> <p>Charset for decoded bytes; an empty value means UTF-8.</p> required <code>is_header</code> <code>bool</code> <p>Whether underscores use RFC 2047 header semantics.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Decoded Unicode text.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Text_Encoding.TextEncoding.decode_base64_string","title":"decode_base64_string  <code>staticmethod</code>","text":"<pre><code>decode_base64_string(encoded_string: str, string_charset: str = 'utf-8') -&gt; str\n</code></pre> <p>Decode Base64 text and convert it to Unicode.</p> <p>Parameters:</p> Name Type Description Default <code>encoded_string</code> <code>str</code> <p>Base64 source text.</p> required <code>string_charset</code> <code>str</code> <p>Charset used to decode the resulting bytes.</p> <code>'utf-8'</code>"},{"location":"reference/generated-utilities/#EMLMailReader.Text_Encoding.TextEncoding.decode_base64_file","title":"decode_base64_file  <code>staticmethod</code>","text":"<pre><code>decode_base64_file(file_contents: str) -&gt; bytes\n</code></pre> <p>Return the binary payload represented by Base64 source text.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Text_Encoding.TextEncoding.decode_header","title":"decode_header  <code>staticmethod</code>","text":"<pre><code>decode_header(encoded_string: str | None, errors: str = 'replace') -&gt; str\n</code></pre> <p>Decode all RFC 2047 encoded words in a header value.</p> <p>Parameters:</p> Name Type Description Default <code>encoded_string</code> <code>str | None</code> <p>Encoded or plain header value. <code>None</code> becomes an empty string.</p> required <code>errors</code> <code>str</code> <p>Error strategy used while converting decoded bytes.</p> <code>'replace'</code> <p>Returns:</p> Type Description <code>str</code> <p>Concatenated Unicode header text.</p>"},{"location":"reference/generated-utilities/#logger","title":"Logger","text":""},{"location":"reference/generated-utilities/#EMLMailReader.Processing_Logs.Logger","title":"EMLMailReader.Processing_Logs.Logger","text":"<p>Configure and write parser diagnostics through Python's root logger.</p> <p>The utility is stateless. Console and file configuration deliberately replace existing root handlers so repeated reader configuration is deterministic.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Processing_Logs.Logger.set_configuration","title":"set_configuration  <code>staticmethod</code>","text":"<pre><code>set_configuration(logging_mode: LoggingMode, target_folder: str = '') -&gt; str\n</code></pre> <p>Configure console, file, or disabled parser logging.</p> <p>Parameters:</p> Name Type Description Default <code>logging_mode</code> <code>LoggingMode</code> <p>Desired :class:<code>LoggingMode</code>.</p> required <code>target_folder</code> <code>str</code> <p>Existing directory for a timestamped log file.</p> <code>''</code> <p>Returns:</p> Type Description <code>str</code> <p>Created log path for file mode, otherwise an empty string.</p> <p>Raises:</p> Type Description <code>FolderNotAvailableError</code> <p>If file mode has no usable target directory.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Processing_Logs.Logger.logentry","title":"logentry  <code>staticmethod</code>","text":"<pre><code>logentry(message: str, logging_level: LoggingLevel | object) -&gt; None\n</code></pre> <p>Write one message at the requested library logging severity.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Log message text.</p> required <code>logging_level</code> <code>LoggingLevel | object</code> <p>Severity mapped to the corresponding Python logging function. Unknown values fall back to debug.</p> required"},{"location":"reference/generated-utilities/#enumerations","title":"Enumerations","text":""},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations","title":"EMLMailReader.Enumerations","text":"<p>Enumerations shared by message parsing, decoding, and logging.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.TransferEncoding","title":"TransferEncoding","text":"<p>               Bases: <code>str</code>, <code>Enum</code></p> <p>Classify standard MIME Content-Transfer-Encoding tokens.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.TransferEncoding.BASE64","title":"BASE64  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BASE64 = 'base64'\n</code></pre> <p>Base64 encoding for binary data and non-ASCII text.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.TransferEncoding.SEVEN_BIT","title":"SEVEN_BIT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>SEVEN_BIT = '7bit'\n</code></pre> <p>7-bit ASCII encoding (default, no encoding needed).</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.TransferEncoding.EIGHT_BIT","title":"EIGHT_BIT  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>EIGHT_BIT = '8bit'\n</code></pre> <p>8-bit encoding for extended ASCII characters.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.TransferEncoding.QUOTED_PRINTABLE","title":"QUOTED_PRINTABLE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>QUOTED_PRINTABLE = 'quoted-printable'\n</code></pre> <p>Quoted-printable encoding for mostly ASCII text with occasional non-ASCII characters.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.TransferEncoding.BINARY","title":"BINARY  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>BINARY = 'binary'\n</code></pre> <p>Unencoded binary MIME content.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.TransferEncoding.UNKNOWN","title":"UNKNOWN  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>UNKNOWN = 'unknown'\n</code></pre> <p>An extension or unrecognized content-transfer-encoding.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingLevel","title":"LoggingLevel","text":"<p>               Bases: <code>Enum</code></p> <p>Select the Python logging severity used by :class:<code>Logger</code>.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingLevel.DEBUG","title":"DEBUG  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>DEBUG = 1\n</code></pre> <p>Detailed diagnostic information for troubleshooting.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingLevel.INFO","title":"INFO  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>INFO = 2\n</code></pre> <p>General informational messages about normal operation.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingLevel.ERROR","title":"ERROR  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>ERROR = 3\n</code></pre> <p>Error conditions that don't prevent continued operation.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingLevel.CRITICAL","title":"CRITICAL  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>CRITICAL = 4\n</code></pre> <p>Serious errors that may cause the application to terminate.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingMode","title":"LoggingMode","text":"<p>               Bases: <code>Enum</code></p> <p>Select whether parser diagnostics are disabled, logged, or written.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingMode.CONSOLE","title":"CONSOLE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>CONSOLE = 1\n</code></pre> <p>Log messages are printed to the console/terminal.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingMode.FILE","title":"FILE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>FILE = 2\n</code></pre> <p>Log messages are written to a log file.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Enumerations.LoggingMode.NONE","title":"NONE  <code>class-attribute</code> <code>instance-attribute</code>","text":"<pre><code>NONE = 3\n</code></pre> <p>Logging is disabled (no output generated).</p>"},{"location":"reference/generated-utilities/#exceptions","title":"Exceptions","text":""},{"location":"reference/generated-utilities/#EMLMailReader.Custom_Exceptions","title":"EMLMailReader.Custom_Exceptions","text":"<p>Exceptions raised by EMLMailReader filesystem operations.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Custom_Exceptions.FileMissingError","title":"FileMissingError","text":"<pre><code>FileMissingError(filePath: str)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Report that an EML source file is unavailable.</p> <p><code>MailReader.get_email</code> catches this exception and normally exposes the failure as a <code>None</code> result, but the type remains public for callers that use lower-level workflows.</p> <p>Attributes:</p> Name Type Description <code>filePath</code> <p>Path that could not be found or accessed.</p> <p>Store the unavailable file path.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Custom_Exceptions.FileMissingError.filePath","title":"filePath  <code>instance-attribute</code>","text":"<pre><code>filePath = filePath\n</code></pre> <p>The file path that was not found or is inaccessible.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Custom_Exceptions.FolderNotAvailableError","title":"FolderNotAvailableError","text":"<pre><code>FolderNotAvailableError(folderPath: str)\n</code></pre> <p>               Bases: <code>Exception</code></p> <p>Report that an output directory is unavailable.</p> <p>Attachment saving and file logging require an existing target directory and raise this exception when that precondition is not met.</p> <p>Attributes:</p> Name Type Description <code>folderPath</code> <p>Directory path that could not be used.</p> <p>Store the unavailable directory path.</p>"},{"location":"reference/generated-utilities/#EMLMailReader.Custom_Exceptions.FolderNotAvailableError.folderPath","title":"folderPath  <code>instance-attribute</code>","text":"<pre><code>folderPath = folderPath\n</code></pre> <p>The folder path that was not found or is inaccessible.</p>"}]}